# Visualisation catalogue

| View | Answers | Source | Rendered in Learn |
|---|---|---|---|
| System context | Who uses the system and which external systems matter? | `01-system-context.mmd`, `06-system-context.dot` | Yes |
| Container | What runs, owns data, and communicates? | `02-container-view.mmd`, `07-container-view.dot` | Yes |
| Sequence | How does one critical use case behave? | `03-create-delivery-sequence.mmd` | Mermaid source |
| Deployment | Where does the system execute and fail? | `04-deployment-and-observability.mmd`, `11-deployment.dot` | Asset available |
| Event recovery | How do retry, DLQ, and replay work? | `05-event-recovery.mmd`, `08-event-recovery.dot` | Yes |
| Logical architecture | How are channel, application, domain, adapter, and platform concerns separated? | `09-logical-architecture.dot` | Asset available |
| Data flow | Where do sensitive data travel, persist, and leave the system? | `10-data-flow.dot` | Asset available |

Mermaid files are convenient editable sources. DOT files are rendered into the public SVG assets used by the application.
