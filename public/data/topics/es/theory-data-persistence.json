[
  {
    "id": "theory-dp-cheatsheet",
    "title": "Guía Rápida de SQL — Referencia Completa de Sintaxis",
    "sections": [
      {
        "heading": "DDL — Lenguaje de Definición de Datos",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- CREATE TABLE\n-- ═══════════════════════════════════════════════\nCREATE TABLE delivery (\n    id              BIGSERIAL PRIMARY KEY,\n    tracking_code   VARCHAR(50) NOT NULL UNIQUE,\n    status          VARCHAR(20) NOT NULL DEFAULT 'PLANNED',\n    driver_id       BIGINT REFERENCES driver(id),\n    route_id        BIGINT,\n    scheduled_at    TIMESTAMPTZ NOT NULL,\n    delivered_at    TIMESTAMPTZ,\n    notes           TEXT,\n    weight_kg       NUMERIC(8,2),\n    is_express      BOOLEAN DEFAULT false,\n    created_at      TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,\n    updated_at      TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,\n    CONSTRAINT fk_route FOREIGN KEY (route_id) REFERENCES route(id),\n    CONSTRAINT chk_weight CHECK (weight_kg > 0)\n);\n\n-- ═══════════════════════════════════════════════\n-- ALTER TABLE\n-- ═══════════════════════════════════════════════\nALTER TABLE delivery ADD COLUMN priority INT DEFAULT 0;\nALTER TABLE delivery DROP COLUMN notes;\nALTER TABLE delivery ALTER COLUMN status SET DEFAULT 'DRAFT';\nALTER TABLE delivery ALTER COLUMN status TYPE VARCHAR(30);\nALTER TABLE delivery RENAME COLUMN is_express TO express;\nALTER TABLE delivery ADD CONSTRAINT uq_tracking UNIQUE (tracking_code);\nALTER TABLE delivery DROP CONSTRAINT chk_weight;\nALTER TABLE delivery RENAME TO shipment;\n\n-- ═══════════════════════════════════════════════\n-- DROP / TRUNCATE\n-- ═══════════════════════════════════════════════\nDROP TABLE IF EXISTS delivery CASCADE;  -- CASCADE drops dependent objects\nTRUNCATE TABLE delivery RESTART IDENTITY CASCADE;  -- rápido delete all filas\n\n-- ═══════════════════════════════════════════════\n-- INDEXES\n-- ═══════════════════════════════════════════════\nCREATE INDEX idx_delivery_status ON delivery(status);\nCREATE INDEX idx_delivery_driver_date ON delivery(driver_id, scheduled_at DESC);\nCREATE UNIQUE INDEX idx_tracking ON delivery(tracking_code);\nCREATE INDEX idx_delivery_notes_gin ON delivery USING GIN (to_tsvector('english', notes));\nCREATE INDEX idx_delivery_active ON delivery(scheduled_at) WHERE status = 'ACTIVE';  -- partial\nCREATE INDEX idx_delivery_lower_code ON delivery(LOWER(tracking_code));  -- expression\nDROP INDEX IF EXISTS idx_delivery_status;\n\n-- ═══════════════════════════════════════════════\n-- VIEWS\n-- ═══════════════════════════════════════════════\nCREATE VIEW active_deliveries AS\n  SELECT id, tracking_code, driver_id, scheduled_at\n  FROM delivery WHERE status = 'ACTIVE';\n\nCREATE MATERIALIZED VIEW daily_stats AS\n  SELECT DATE(scheduled_at) AS day, COUNT(*) AS total, AVG(weight_kg) AS avg_weight\n  FROM delivery GROUP BY DATE(scheduled_at);\n\nREFRESH MATERIALIZED VIEW daily_stats;  -- debe refresh manually\nREFRESH MATERIALIZED VIEW CONCURRENTLY daily_stats;  -- no bloqueo (needs unique índice)\n\n-- ═══════════════════════════════════════════════\n-- SEQUENCES\n-- ═══════════════════════════════════════════════\nCREATE SEQUENCE delivery_id_seq START 1 INCREMENT 1;\nSELECT nextval('delivery_id_seq');\nSELECT currval('delivery_id_seq');\nALTER SEQUENCE delivery_id_seq RESTART WITH 1000;</code></pre>"
      },
      {
        "heading": "DML — Lenguaje de Manipulación de Datos",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- INSERT\n-- ═══════════════════════════════════════════════\nINSERT INTO delivery (tracking_code, status, driver_id, scheduled_at)\nVALUES ('TRK-001', 'PLANNED', 42, '2026-07-21 08:00');\n\n-- Insert varias filas\nINSERT INTO delivery (tracking_code, status, scheduled_at) VALUES\n  ('TRK-002', 'PLANNED', '2026-07-21 09:00'),\n  ('TRK-003', 'PLANNED', '2026-07-21 10:00'),\n  ('TRK-004', 'PLANNED', '2026-07-21 11:00');\n\n-- Insert desde SELECT\nINSERT INTO delivery_archive (id, tracking_code, status)\n  SELECT id, tracking_code, status FROM delivery WHERE status = 'DELIVERED';\n\n-- UPSERT (INSERT ... ON CONFLICT)\nINSERT INTO delivery (tracking_code, status, scheduled_at)\nVALUES ('TRK-001', 'DISPATCHED', NOW())\nON CONFLICT (tracking_code)\nDO UPDATE SET status = EXCLUDED.status, updated_at = NOW();\n\nINSERT INTO delivery (tracking_code, status)\nVALUES ('TRK-001', 'PLANNED')\nON CONFLICT (tracking_code) DO NOTHING;  -- skip if exists\n\n-- RETURNING (PostgreSQL) — get inserted/updated filas back\nINSERT INTO delivery (tracking_code, status, scheduled_at)\nVALUES ('TRK-005', 'PLANNED', NOW())\nRETURNING id, tracking_code;\n\n-- ═══════════════════════════════════════════════\n-- UPDATE\n-- ═══════════════════════════════════════════════\nUPDATE delivery SET status = 'DISPATCHED', updated_at = NOW()\nWHERE id = 123;\n\nUPDATE delivery SET status = 'CANCELLED'\nWHERE scheduled_at < NOW() - INTERVAL '7 days' AND status = 'PLANNED';\n\n-- Update con subquery\nUPDATE delivery d SET driver_id = (\n  SELECT id FROM driver WHERE region = d.region ORDER BY workload LIMIT 1\n) WHERE d.driver_id IS NULL;\n\n-- Update con JOIN (PostgreSQL syntax)\nUPDATE delivery d SET status = 'ASSIGNED'\nFROM driver dr\nWHERE d.driver_id = dr.id AND dr.available = true;\n\n-- Update RETURNING\nUPDATE delivery SET status = 'DISPATCHED'\nWHERE id = 123 RETURNING id, status, driver_id;\n\n-- ═══════════════════════════════════════════════\n-- DELETE\n-- ═══════════════════════════════════════════════\nDELETE FROM delivery WHERE id = 123;\nDELETE FROM delivery WHERE status = 'CANCELLED' AND created_at < '2025-01-01';\n\n-- Delete con subquery\nDELETE FROM delivery WHERE driver_id IN (\n  SELECT id FROM driver WHERE terminated = true\n);\n\n-- Delete RETURNING\nDELETE FROM delivery WHERE status = 'CANCELLED' RETURNING id, tracking_code;\n\n-- Delete in batches (evitar long bloqueos)\nWITH lote AS (\n  SELECT id FROM delivery WHERE status = 'ARCHIVED' LIMIT 1000\n)\nDELETE FROM delivery WHERE id IN (SELECT id FROM lote);</code></pre>"
      },
      {
        "heading": "SELECT — Consultas y Filtrado",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- BASIC SELECT\n-- ═══════════════════════════════════════════════\nSELECT * FROM delivery;                         -- all columnas (evitar in producción!)\nSELECT id, status, scheduled_at FROM delivery;  -- explicit columnas\nSELECT DISTINCT status FROM delivery;           -- unique valores\nSELECT DISTINCT ON (driver_id) * FROM delivery  -- primero fila por driver (PostgreSQL)\n  ORDER BY driver_id, scheduled_at DESC;\n\n-- ═══════════════════════════════════════════════\n-- WHERE — Filtering\n-- ═══════════════════════════════════════════════\nWHERE status = 'ACTIVE'                     -- equality\nWHERE status != 'CANCELLED'                 -- not equal (también <>)\nWHERE weight_kg > 10                        -- comparison: >, <, >=, <=\nWHERE weight_kg BETWEEN 5 AND 20            -- rango (inclusive)\nWHERE status IN ('PLANNED', 'DISPATCHED')   -- set membership\nWHERE status NOT IN ('CANCELLED', 'FAILED') -- exclusion\nWHERE driver_id IS NULL                     -- null check\nWHERE driver_id IS NOT NULL                 -- not null\nWHERE tracking_code LIKE 'TRK-%'           -- pattern (% = any chars)\nWHERE tracking_code LIKE 'TRK-00_'         -- pattern (_ = single char)\nWHERE tracking_code ILIKE 'trk-%'          -- case-insensitive (PostgreSQL)\nWHERE notes SIMILAR TO '%(urgent|express)%' -- regex-like (PostgreSQL)\nWHERE scheduled_at >= CURRENT_DATE          -- date comparison\nWHERE scheduled_at >= NOW() - INTERVAL '7 days'  -- relative date\n\n-- Compound condiciones\nWHERE status = 'ACTIVE' AND driver_id IS NOT NULL\nWHERE status = 'FAILED' OR (status = 'PLANNED' AND scheduled_at < NOW())\nWHERE NOT (status = 'CANCELLED')\n\n-- ═══════════════════════════════════════════════\n-- ORDER BY\n-- ═══════════════════════════════════════════════\nORDER BY scheduled_at ASC                   -- ascending (predeterminado)\nORDER BY scheduled_at DESC                  -- descending\nORDER BY status ASC, scheduled_at DESC      -- multi-columna\nORDER BY CASE status                        -- custom order\n  WHEN 'URGENT' THEN 1\n  WHEN 'ACTIVE' THEN 2\n  ELSE 3 END\nORDER BY scheduled_at NULLS LAST            -- nulls at end\n\n-- ═══════════════════════════════════════════════\n-- LIMIT & OFFSET (pagination)\n-- ═══════════════════════════════════════════════\nLIMIT 50                      -- primero 50 filas\nLIMIT 50 OFFSET 100           -- skip 100, devolver next 50\nFETCH FIRST 10 ROWS ONLY     -- SQL standard syntax\nFETCH NEXT 10 ROWS ONLY      -- mismo thing</code></pre>"
      },
      {
        "heading": "JOINs",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- JOIN TYPES\n-- ═══════════════════════════════════════════════\n-- INNER JOIN: only coincidentes filas\nSELECT d.id, dr.name\nFROM delivery d\nINNER JOIN driver dr ON d.driver_id = dr.id;\n\n-- LEFT JOIN: all desde izquierda, NULL if no match on derecha\nSELECT d.id, dr.name\nFROM delivery d\nLEFT JOIN driver dr ON d.driver_id = dr.id;\n\n-- RIGHT JOIN: all desde derecha (rara vez utilizado)\nSELECT d.id, dr.name\nFROM delivery d\nRIGHT JOIN driver dr ON d.driver_id = dr.id;\n\n-- FULL OUTER JOIN: all desde both sides\nSELECT d.id, dr.name\nFROM delivery d\nFULL OUTER JOIN driver dr ON d.driver_id = dr.id;\n\n-- CROSS JOIN: cartesian product (cada combo)\nSELECT d.id, ts.slot\nFROM delivery d\nCROSS JOIN time_slot ts;\n\n-- SELF JOIN: tabla joined to itself\nSELECT d1.id, d2.id AS next_delivery\nFROM delivery d1\nJOIN delivery d2 ON d1.driver_id = d2.driver_id\n  AND d2.scheduled_at > d1.scheduled_at;\n\n-- LATERAL JOIN: subquery puede reference outer tabla (PostgreSQL)\nSELECT d.id, latest_event.event_type\nFROM delivery d\nLEFT JOIN LATERAL (\n  SELECT event_type FROM delivery_event e\n  WHERE e.delivery_id = d.id\n  ORDER BY created_at DESC LIMIT 1\n) latest_event ON true;\n\n-- Multiple JOINs\nSELECT d.id, dr.name, r.region, COUNT(ds.id) AS stop_count\nFROM delivery d\nJOIN driver dr ON d.driver_id = dr.id\nJOIN route r ON d.route_id = r.id\nLEFT JOIN delivery_stop ds ON ds.delivery_id = d.id\nGROUP BY d.id, dr.name, r.region;\n\n-- ═══════════════════════════════════════════════\n-- EXISTS / NOT EXISTS (correlated subquery)\n-- ═══════════════════════════════════════════════\nSELECT d.* FROM delivery d\nWHERE EXISTS (\n  SELECT 1 FROM delivery_stop ds\n  WHERE ds.delivery_id = d.id AND ds.status = 'FAILED'\n);\n\nSELECT d.* FROM delivery d\nWHERE NOT EXISTS (\n  SELECT 1 FROM delivery_event e\n  WHERE e.delivery_id = d.id AND e.event_type = 'CONFIRMED'\n);</code></pre>"
      },
      {
        "heading": "Agregación — GROUP BY, HAVING y Funciones de Agregación",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- AGGREGATE FUNCTIONS\n-- ═══════════════════════════════════════════════\nSELECT\n  COUNT(*)                    AS total_rows,\n  COUNT(driver_id)            AS assigned_count,   -- excludes NULL\n  COUNT(DISTINCT driver_id)   AS unique_drivers,\n  SUM(weight_kg)              AS total_weight,\n  AVG(weight_kg)              AS avg_weight,\n  MIN(scheduled_at)           AS earliest,\n  MAX(scheduled_at)           AS latest,\n  BOOL_OR(is_express)         AS has_any_express,  -- PostgreSQL\n  ARRAY_AGG(tracking_code)    AS all_codes,        -- PostgreSQL\n  STRING_AGG(status, ', ')    AS statuses          -- PostgreSQL\nFROM delivery;\n\n-- ═══════════════════════════════════════════════\n-- GROUP BY\n-- ═══════════════════════════════════════════════\nSELECT status, COUNT(*) AS total\nFROM delivery\nGROUP BY status;\n\nSELECT driver_id, DATE(scheduled_at) AS day, COUNT(*) AS deliveries\nFROM delivery\nGROUP BY driver_id, DATE(scheduled_at)\nORDER BY day, deliveries DESC;\n\n-- GROUP BY con expression\nSELECT\n  CASE WHEN weight_kg < 5 THEN 'light'\n       WHEN weight_kg < 20 THEN 'medium'\n       ELSE 'heavy' END AS category,\n  COUNT(*)\nFROM delivery\nGROUP BY 1;  -- reference by position\n\n-- GROUPING SETS / ROLLUP / CUBE (advanced aggregation)\nSELECT status, driver_id, COUNT(*)\nFROM delivery\nGROUP BY GROUPING SETS (\n  (status, driver_id),  -- detail\n  (status),             -- subtotal by status\n  ()                    -- grand total\n);\n\nSELECT status, driver_id, COUNT(*)\nFROM delivery\nGROUP BY ROLLUP (status, driver_id);  -- hierarchical subtotals\n\n-- ═══════════════════════════════════════════════\n-- HAVING — filter on aggregates\n-- ═══════════════════════════════════════════════\nSELECT driver_id, COUNT(*) AS total\nFROM delivery\nGROUP BY driver_id\nHAVING COUNT(*) > 10;  -- only drivers con 10+ deliveries\n\nSELECT status, AVG(weight_kg) AS avg_w\nFROM delivery\nGROUP BY status\nHAVING AVG(weight_kg) > 15;</code></pre>"
      },
      {
        "heading": "Funciones de Ventana",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- WINDOW FUNCTIONS — compute entre filas sin collapsing\n-- ═══════════════════════════════════════════════\n\n-- ROW_NUMBER: sequential number within partición\nSELECT id, driver_id, scheduled_at,\n  ROW_NUMBER() OVER (PARTITION BY driver_id ORDER BY scheduled_at) AS seq\nFROM delivery;\n\n-- RANK: mismo rank for ties, gaps después\nSELECT id, weight_kg,\n  RANK() OVER (ORDER BY weight_kg DESC) AS weight_rank\nFROM delivery;\n\n-- DENSE_RANK: mismo rank for ties, NO gaps\nSELECT id, weight_kg,\n  DENSE_RANK() OVER (ORDER BY weight_kg DESC) AS weight_rank\nFROM delivery;\n\n-- NTILE: divide en N equal buckets\nSELECT id, weight_kg,\n  NTILE(4) OVER (ORDER BY weight_kg) AS quartile\nFROM delivery;\n\n-- LAG / LEAD: access previous/next fila\nSELECT id, scheduled_at,\n  LAG(scheduled_at) OVER (PARTITION BY driver_id ORDER BY scheduled_at) AS prev_delivery,\n  LEAD(scheduled_at) OVER (PARTITION BY driver_id ORDER BY scheduled_at) AS next_delivery,\n  scheduled_at - LAG(scheduled_at) OVER (PARTITION BY driver_id ORDER BY scheduled_at) AS gap\nFROM delivery;\n\n-- FIRST_VALUE / LAST_VALUE / NTH_VALUE\nSELECT id, driver_id, scheduled_at,\n  FIRST_VALUE(id) OVER (PARTITION BY driver_id ORDER BY scheduled_at) AS first_delivery,\n  LAST_VALUE(id) OVER (\n    PARTITION BY driver_id ORDER BY scheduled_at\n    ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n  ) AS last_delivery\nFROM delivery;\n\n-- Running totals / moving averages\nSELECT id, weight_kg,\n  SUM(weight_kg) OVER (ORDER BY id) AS running_total,\n  AVG(weight_kg) OVER (ORDER BY id ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) AS moving_avg_3\nFROM delivery;\n\n-- Percent of total\nSELECT driver_id, COUNT(*) AS cnt,\n  ROUND(100.0 * COUNT(*) / SUM(COUNT(*)) OVER (), 1) AS pct\nFROM delivery GROUP BY driver_id;\n\n-- Named WINDOW (reuse definition)\nSELECT id, driver_id, scheduled_at,\n  ROW_NUMBER() OVER w AS seq,\n  LAG(scheduled_at) OVER w AS prev\nFROM delivery\nWINDOW w AS (PARTITION BY driver_id ORDER BY scheduled_at);</code></pre>"
      },
      {
        "heading": "Subconsultas y CTE",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- SUBQUERIES\n-- ═══════════════════════════════════════════════\n-- Scalar subquery (devuelve single valor)\nSELECT id, weight_kg,\n  weight_kg - (SELECT AVG(weight_kg) FROM delivery) AS diff_from_avg\nFROM delivery;\n\n-- IN subquery\nSELECT * FROM delivery\nWHERE driver_id IN (SELECT id FROM driver WHERE region = 'NORTH');\n\n-- Correlated subquery (references outer consulta)\nSELECT d.* FROM delivery d\nWHERE d.weight_kg > (\n  SELECT AVG(d2.weight_kg) FROM delivery d2\n  WHERE d2.driver_id = d.driver_id  -- references outer 'd'\n);\n\n-- FROM subquery (derived tabla)\nSELECT driver_id, avg_weight\nFROM (\n  SELECT driver_id, AVG(weight_kg) AS avg_weight\n  FROM delivery GROUP BY driver_id\n) sub\nWHERE avg_weight > 15;\n\n-- ═══════════════════════════════════════════════\n-- CTEs (Common Table Expressions) — WITH clause\n-- ═══════════════════════════════════════════════\nWITH active_drivers AS (\n  SELECT DISTINCT driver_id FROM delivery WHERE status = 'ACTIVE'\n),\ndriver_stats AS (\n  SELECT driver_id, COUNT(*) AS total, AVG(weight_kg) AS avg_w\n  FROM delivery\n  WHERE driver_id IN (SELECT driver_id FROM active_drivers)\n  GROUP BY driver_id\n)\nSELECT ds.*, dr.name\nFROM driver_stats ds\nJOIN driver dr ON ds.driver_id = dr.id\nORDER BY ds.total DESC;\n\n-- Recursive CTE (hierarchical datos)\nWITH RECURSIVE org_tree AS (\n  -- Base case: top-level managers\n  SELECT id, name, manager_id, 1 AS level\n  FROM employee WHERE manager_id IS NULL\n  UNION ALL\n  -- Recursive step: employees bajo each manager\n  SELECT e.id, e.name, e.manager_id, t.level + 1\n  FROM employee e\n  JOIN org_tree t ON e.manager_id = t.id\n)\nSELECT * FROM org_tree ORDER BY level, name;\n\n-- CTE con INSERT (writable CTE — PostgreSQL)\nWITH moved AS (\n  DELETE FROM delivery WHERE status = 'ARCHIVED' RETURNING *\n)\nINSERT INTO delivery_archive SELECT * FROM moved;</code></pre>"
      },
      {
        "heading": "Operaciones de Conjuntos y CASE",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- SET OPERATIONS\n-- ═══════════════════════════════════════════════\n-- UNION: combine results, remove duplicates\nSELECT id, tracking_code FROM delivery WHERE status = 'URGENT'\nUNION\nSELECT id, tracking_code FROM delivery WHERE is_express = true;\n\n-- UNION ALL: combine results, keep duplicates (más rápido!)\nSELECT id, 'delivery' AS source FROM delivery\nUNION ALL\nSELECT id, 'devolver' AS source FROM delivery_return;\n\n-- INTERSECT: filas in BOTH consultas\nSELECT driver_id FROM delivery WHERE status = 'ACTIVE'\nINTERSECT\nSELECT driver_id FROM delivery WHERE is_express = true;\n\n-- EXCEPT: filas in primero but NOT in segundo\nSELECT driver_id FROM driver\nEXCEPT\nSELECT DISTINCT driver_id FROM delivery WHERE status = 'FAILED';\n\n-- ═══════════════════════════════════════════════\n-- CASE — conditional logic\n-- ═══════════════════════════════════════════════\n-- Simple CASE\nSELECT id,\n  CASE status\n    WHEN 'PLANNED' THEN '📋 Planned'\n    WHEN 'DISPATCHED' THEN '🚚 On the way'\n    WHEN 'DELIVERED' THEN '✅ Done'\n    ELSE '❓ Unknown'\n  END AS status_label\nFROM delivery;\n\n-- Searched CASE (con condiciones)\nSELECT id, weight_kg,\n  CASE\n    WHEN weight_kg < 5 THEN 'light'\n    WHEN weight_kg < 20 THEN 'medium'\n    WHEN weight_kg < 50 THEN 'heavy'\n    ELSE 'oversized'\n  END AS weight_category\nFROM delivery;\n\n-- CASE in WHERE/ORDER BY\nSELECT * FROM delivery\nORDER BY CASE WHEN status = 'URGENT' THEN 0 ELSE 1 END, scheduled_at;\n\n-- COALESCE: primero non-NULL valor\nSELECT COALESCE(driver_id, -1) AS driver_id FROM delivery;\nSELECT COALESCE(notes, 'No notes') AS notes FROM delivery;\n\n-- NULLIF: devuelve NULL if valores are equal\nSELECT NULLIF(status, 'UNKNOWN') FROM delivery;  -- treat 'UNKNOWN' as NULL\n\n-- GREATEST / LEAST\nSELECT GREATEST(scheduled_at, created_at) AS latest_date FROM delivery;</code></pre>"
      },
      {
        "heading": "Funciones de Fecha y Hora",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- DATE/TIME — PostgreSQL\n-- ═══════════════════════════════════════════════\n-- Current timestamps\nSELECT NOW();                           -- 2026-07-21 15:30:00+02\nSELECT CURRENT_TIMESTAMP;               -- mismo as NOW()\nSELECT CURRENT_DATE;                    -- 2026-07-21\nSELECT CURRENT_TIME;                    -- 15:30:00+02\nSELECT CLOCK_TIMESTAMP();               -- real clock (changes within statement)\n\n-- Extract parts\nSELECT EXTRACT(YEAR FROM scheduled_at) FROM delivery;\nSELECT EXTRACT(MONTH FROM scheduled_at) FROM delivery;\nSELECT EXTRACT(DOW FROM scheduled_at) FROM delivery;   -- 0=Sun, 6=Sat\nSELECT EXTRACT(EPOCH FROM scheduled_at) FROM delivery; -- unix timestamp\nSELECT DATE_PART('hour', scheduled_at) FROM delivery;\n\n-- Date arithmetic\nSELECT scheduled_at + INTERVAL '2 hours' FROM delivery;\nSELECT scheduled_at - INTERVAL '30 days' FROM delivery;\nSELECT scheduled_at + 7 * INTERVAL '1 day' FROM delivery;\nSELECT AGE(delivered_at, scheduled_at) FROM delivery;   -- interval entre\nSELECT NOW() - scheduled_at AS time_since FROM delivery;\n\n-- Truncate to precision\nSELECT DATE_TRUNC('month', scheduled_at) FROM delivery; -- primero of month\nSELECT DATE_TRUNC('hour', scheduled_at) FROM delivery;  -- start of hour\nSELECT DATE_TRUNC('week', CURRENT_DATE);                -- monday of week\n\n-- Format\nSELECT TO_CHAR(scheduled_at, 'YYYY-MM-DD HH24:MI:SS') FROM delivery;\nSELECT TO_CHAR(scheduled_at, 'Day, DD Mon YYYY') FROM delivery;\n\n-- Parse\nSELECT TO_TIMESTAMP('2026-07-21 15:30:00', 'YYYY-MM-DD HH24:MI:SS');\nSELECT TO_DATE('21/07/2026', 'DD/MM/YYYY');\n\n-- Generate series (útil for reports con gaps)\nSELECT generate_series(\n  '2026-07-01'::date, '2026-07-31'::date, '1 day'::interval\n) AS day;\n\n-- Date ranges (PostgreSQL)\nSELECT * FROM delivery\nWHERE scheduled_at &lt;@ tstzrange('2026-07-01', '2026-08-01');  -- contained in rango</code></pre>"
      },
      {
        "heading": "Funciones de Cadenas",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- STRING FUNCTIONS\n-- ═══════════════════════════════════════════════\nSELECT LENGTH('hello');                    -- 5\nSELECT UPPER('hello');                     -- HELLO\nSELECT LOWER('HELLO');                     -- hello\nSELECT INITCAP('hello world');             -- Hello World\nSELECT TRIM('  hello  ');                  -- 'hello'\nSELECT LTRIM('  hello');                   -- 'hello'\nSELECT RTRIM('hello  ');                   -- 'hello'\nSELECT REPLACE('hello world', 'world', 'SQL');  -- 'hello SQL'\nSELECT SUBSTRING('hello' FROM 2 FOR 3);   -- 'ell'\nSELECT LEFT('hello', 3);                   -- 'hel'\nSELECT RIGHT('hello', 3);                  -- 'llo'\nSELECT POSITION('lo' IN 'hello');          -- 4\nSELECT SPLIT_PART('a.b.c', '.', 2);       -- 'b'\nSELECT REPEAT('ha', 3);                   -- 'hahaha'\nSELECT REVERSE('hello');                   -- 'olleh'\nSELECT LPAD('42', 5, '0');                -- '00042'\nSELECT RPAD('hi', 5, '.');                -- 'hi...'\n\n-- Concatenation\nSELECT 'Hello' || ' ' || 'World';         -- 'Hello World'\nSELECT CONCAT('Hello', ' ', 'World');      -- 'Hello World' (handles NULL)\nSELECT CONCAT_WS(', ', 'a', 'b', 'c');   -- 'a, b, c'\n\n-- Pattern coincidentes\nSELECT 'hello' LIKE 'h%';                 -- true\nSELECT 'hello' SIMILAR TO 'h(e|a)llo';   -- true (PostgreSQL regex-like)\nSELECT 'hello' ~ '^h.*o$';               -- true (POSIX regex)\nSELECT REGEXP_REPLACE('abc123', '[0-9]+', 'X');  -- 'abcX'\nSELECT REGEXP_MATCHES('abc123def456', '[0-9]+', 'g');  -- {123}, {456}\n\n-- Type casting\nSELECT CAST('42' AS INTEGER);             -- 42\nSELECT '42'::INTEGER;                     -- 42 (PostgreSQL shorthand)\nSELECT 42::TEXT;                           -- '42'</code></pre>"
      },
      {
        "heading": "Transacciones y Bloqueos",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- TRANSACTIONS\n-- ═══════════════════════════════════════════════\nBEGIN;  -- or START TRANSACTION\n  UPDATE delivery SET status = 'DISPATCHED' WHERE id = 123;\n  INSERT INTO delivery_event (delivery_id, event_type) VALUES (123, 'DISPATCHED');\nCOMMIT;\n\nBEGIN;\n  UPDATE account SET balance = balance - 100 WHERE id = 1;\n  UPDATE account SET balance = balance + 100 WHERE id = 2;\n  -- something goes wrong:\nROLLBACK;  -- undo everything\n\n-- SAVEPOINT (partial rollback)\nBEGIN;\n  INSERT INTO delivery (tracking_code) VALUES ('TRK-A');\n  SAVEPOINT sp1;\n  INSERT INTO delivery (tracking_code) VALUES ('TRK-B');\n  ROLLBACK TO sp1;  -- undo TRK-B only\n  INSERT INTO delivery (tracking_code) VALUES ('TRK-C');\nCOMMIT;  -- TRK-A and TRK-C are committed\n\n-- Isolation levels\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;    -- PostgreSQL predeterminado\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ;\nSET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\n\nBEGIN ISOLATION LEVEL SERIALIZABLE;\n  -- all lecturas see a consistente instantánea\n  -- conflictos are detected at commit\nCOMMIT;\n\n-- ═══════════════════════════════════════════════\n-- LOCKING\n-- ═══════════════════════════════════════════════\n-- SELECT FOR UPDATE: bloqueo filas for modification\nSELECT * FROM delivery WHERE id = 123 FOR UPDATE;\n\n-- SKIP LOCKED: skip filas locked by other transacciones (queue pattern)\nSELECT * FROM delivery\nWHERE status = 'PENDING'\nORDER BY created_at\nLIMIT 1\nFOR UPDATE SKIP LOCKED;\n\n-- NOWAIT: fail inmediatamente if fila is locked\nSELECT * FROM delivery WHERE id = 123 FOR UPDATE NOWAIT;\n\n-- Advisory bloqueos (aplicación-level)\nSELECT pg_advisory_lock(12345);      -- blocking\nSELECT pg_try_advisory_lock(12345);  -- non-blocking, devuelve bool\nSELECT pg_advisory_unlock(12345);</code></pre>"
      },
      {
        "heading": "Características Específicas de PostgreSQL",
        "content": "<pre><code>-- ═══════════════════════════════════════════════\n-- JSONB (PostgreSQL)\n-- ═══════════════════════════════════════════════\n-- Store and consulta JSON\nCREATE TABLE delivery_meta (\n  delivery_id BIGINT PRIMARY KEY,\n  metadata    JSONB NOT NULL DEFAULT '{}'\n);\n\nINSERT INTO delivery_meta VALUES (1, '{\"priority\": \"alto\", \"tags\": [\"express\", \"fragile\"]}');\n\n-- Access fields\nSELECT metadata->>'priority' FROM delivery_meta;           -- text: 'alto'\nSELECT metadata->'tags' FROM delivery_meta;                -- jsonb: [\"express\",\"fragile\"]\nSELECT metadata->'tags'->>0 FROM delivery_meta;            -- text: 'express'\nSELECT metadata #>> '{tags,0}' FROM delivery_meta;         -- text: 'express' (path)\n\n-- Filter\nSELECT * FROM delivery_meta WHERE metadata->>'priority' = 'alto';\nSELECT * FROM delivery_meta WHERE metadata @> '{\"priority\": \"alto\"}';\nSELECT * FROM delivery_meta WHERE metadata ? 'priority';   -- clave exists\nSELECT * FROM delivery_meta WHERE metadata->'tags' ? 'express'; -- array contiene\n\n-- Update JSONB\nUPDATE delivery_meta SET metadata = metadata || '{\"urgent\": true}';    -- merge\nUPDATE delivery_meta SET metadata = metadata - 'priority';             -- remove clave\nUPDATE delivery_meta SET metadata = jsonb_set(metadata, '{priority}', '\"bajo\"');\n\n-- Index JSONB\nCREATE INDEX idx_meta_gin ON delivery_meta USING GIN (metadata);\nCREATE INDEX idx_meta_priority ON delivery_meta ((metadata->>'priority'));\n\n-- ═══════════════════════════════════════════════\n-- ARRAYS (PostgreSQL)\n-- ═══════════════════════════════════════════════\nSELECT ARRAY[1, 2, 3];                    -- {1,2,3}\nSELECT ARRAY_AGG(id) FROM delivery;       -- aggregate en array\nSELECT UNNEST(ARRAY[1,2,3]);              -- expand array to filas\nSELECT * FROM delivery WHERE 42 = ANY(stop_ids);  -- array contiene\nSELECT * FROM delivery WHERE stop_ids @> ARRAY[42]; -- contiene\nSELECT ARRAY_LENGTH(stop_ids, 1) FROM delivery;  -- array length\n\n-- ═══════════════════════════════════════════════\n-- PARTITIONING (PostgreSQL 10+)\n-- ═══════════════════════════════════════════════\nCREATE TABLE events (\n  id BIGSERIAL, event_time TIMESTAMPTZ, payload JSONB\n) PARTITION BY RANGE (event_time);\n\nCREATE TABLE events_2026_q3 PARTITION OF events\n  FOR VALUES FROM ('2026-07-01') TO ('2026-10-01');\n\n-- List partición\nCREATE TABLE delivery_by_region (\n  id BIGSERIAL, region TEXT, datos JSONB\n) PARTITION BY LIST (region);\n\nCREATE TABLE delivery_north PARTITION OF delivery_by_region\n  FOR VALUES IN ('NORTH', 'NORTHEAST');\n\n-- ═══════════════════════════════════════════════\n-- OTHER USEFUL PostgreSQL FEATURES\n-- ═══════════════════════════════════════════════\n-- EXPLAIN ANALYZE: show plan de ejecución con real timing\nEXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)\n  SELECT * FROM delivery WHERE status = 'ACTIVE';\n\n-- Copy (bulk import/export)\nCOPY delivery FROM '/path/to/file.csv' WITH (FORMAT csv, HEADER true);\nCOPY (SELECT * FROM delivery WHERE status = 'ACTIVE') TO '/tmp/active.csv' CSV HEADER;\n\n-- Generate series\nSELECT * FROM generate_series(1, 10) AS n;  -- numbers 1..10\nSELECT * FROM generate_series('2026-01-01'::date, '2026-12-31'::date, '1 month');\n\n-- Conditional aggregation (pivot-like)\nSELECT driver_id,\n  COUNT(*) FILTER (WHERE status = 'DELIVERED') AS delivered,\n  COUNT(*) FILTER (WHERE status = 'FAILED') AS failed,\n  COUNT(*) FILTER (WHERE status = 'ACTIVE') AS active\nFROM delivery GROUP BY driver_id;</code></pre>"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-sql-fundamentals"
  },
  {
    "id": "theory-dp-joins",
    "title": "JOINs SQL y Patrones de Consulta — De lo Básico a Producción",
    "sections": [
      {
        "heading": "Tipos de JOIN Visualizados",
        "content": "```\nINNER JOIN:  Solo las filas coincidentes en AMBAS tablas\nLEFT JOIN:   Todas las filas de la IZQUIERDA más las coincidentes de la derecha (NULL si no hay coincidencia)\nRIGHT JOIN:  Todas las filas de la DERECHA más las coincidentes de la izquierda (se usa rara vez)\nFULL JOIN:   Todas las filas de AMBAS tablas (NULL donde no hay coincidencia)\nCROSS JOIN:  Producto cartesiano (cada fila × cada fila)\n```\n\n```sql\n-- INNER: only orders WITH a customer\nSELECT o.*, c.name\nFROM orders o\nINNER JOIN customers c ON o.customer_id = c.id;\n\n-- LEFT: ALL orders, even if customer was deleted\nSELECT o.*, c.name\nFROM orders o\nLEFT JOIN customers c ON o.customer_id = c.id;\n-- c.name = NULL cuando customer doesn't exist\n```"
      },
      {
        "heading": "Self-JOIN para Jerarquías",
        "content": "When a tabla references itself (employee → manager, category → parent_category):\n\n```sql\n-- Buscar employees and their managers\nSELECT e.name AS employee, m.name AS manager\nFROM employee e\nLEFT JOIN employee m ON e.manager_id = m.id;\n\n-- Recursive CTE for full hierarchy (PostgreSQL)\nWITH RECURSIVE árbol AS (\n    SELECT id, name, parent_id, 0 AS depth\n    FROM category WHERE parent_id IS NULL\n    UNION ALL\n    SELECT c.id, c.name, c.parent_id, t.depth + 1\n    FROM category c\n    JOIN árbol t ON c.parent_id = t.id\n)\nSELECT * FROM árbol ORDER BY depth, name;\n```"
      },
      {
        "heading": "LATERAL JOIN — Subconsulta Correlacionada como JOIN",
        "content": "PostgreSQL-specific but extremely powerful. Like a subquery that puede reference the outer tabla:\n\n```sql\n-- Top 3 comments por post (LATERAL)\nSELECT p.title, c.review, c.score\nFROM post p\nCROSS JOIN LATERAL (\n    SELECT review, score\n    FROM post_comment\n    WHERE post_id = p.id\n    ORDER BY score DESC\n    LIMIT 3\n) c;\n```\n\nEquivalent to a correlated subquery but puede devolver varias columnas/filas. Much más readable than window function + filter approaches."
      },
      {
        "heading": "Patrones Anti-JOIN",
        "content": "Buscar filas que NO tienen coincidencia:\n\n```sql\n-- ✅ NOT EXISTS (normalmente la opción más rápida: se detiene en la primera coincidencia)\nSELECT p.*\nFROM post p\nWHERE NOT EXISTS (\n    SELECT 1 FROM post_comment c WHERE c.post_id = p.id\n);\n\n-- ✅ LEFT JOIN + IS NULL (también es rápido con una indexación adecuada)\nSELECT p.*\nFROM post p\nLEFT JOIN post_comment c ON c.post_id = p.id\nWHERE c.id IS NULL;\n\n-- ❌ NOT IN (peligroso con valores NULL: devuelve vacío si la subconsulta contiene NULL!)\nSELECT * FROM post\nWHERE id NOT IN (SELECT post_id FROM post_comment);  -- BUG if any post_id is NULL\n```"
      },
      {
        "heading": "Patrones de Agregación con GROUP BY",
        "content": "```sql\n-- Count comments por post\nSELECT p.id, p.title, COUNT(c.id) AS comment_count\nFROM post p\nLEFT JOIN post_comment c ON c.post_id = p.id\nGROUP BY p.id, p.title\nHAVING COUNT(c.id) > 5\nORDER BY comment_count DESC;\n\n-- Conditional aggregation (pivot-like)\nSELECT\n    date_trunc('month', created_on) AS month,\n    COUNT(*) FILTER (WHERE status = 'PENDING') AS pending,\n    COUNT(*) FILTER (WHERE status = 'SHIPPED') AS shipped,\n    COUNT(*) FILTER (WHERE status = 'DELIVERED') AS delivered\nFROM orders\nGROUP BY 1\nORDER BY 1;\n```\n\n`FILTER (WHERE ...)` is PostgreSQL-specific but very clean for conditional counts."
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-joins-patterns"
  },
  {
    "id": "theory-dp-indexes",
    "title": "Índices y EXPLAIN — Cómo Acelerar Consultas",
    "sections": [
      {
        "heading": "Cómo Funcionan los Índices B-Tree",
        "content": "A B-Tree índice is a ordenado, balanced árbol structure donde:\n- **Leaf nodos** contener pointers to real tabla filas (heap tuples)\n- **Internal nodos** guide the search (like a phone book)\n- **O(log n) búsquedas** — finding a fila in 1 billion registros takes ~30 página lecturas\n\n```sql\nCREATE INDEX idx_post_created ON post(created_on);\n\n-- This consulta puede use the índice:\nSELECT * FROM post WHERE created_on > '2024-01-01';\n\n-- The índice stores created_on valores SORTED, so rango escaneos are rápido\n```\n\n**Sin índice:** Escaneo completo de la tabla — leer cada single fila (O(n)).\n**Con índice:** Recorrido del B-Tree — jump directamente to coincidentes filas (O(log n))."
      },
      {
        "heading": "Índices Compuestos — El Orden de las Columnas Importa",
        "content": "```sql\nCREATE INDEX idx_order_status_date ON orders(status, created_on);\n```\n\n**Regla del prefijo más a la izquierda:** The índice is útil for consultas that filter on:\n- `status` alone ✅\n- `status` AND `created_on` ✅\n- `created_on` alone ❌ (índice not utilizado!)\n\n**Heurística del orden:** Put primero las condiciones de igualdad y al final las condiciones de rango:\n```sql\n-- ✅ Optimal: equality (status) primero, rango (created_on) segundo\nWHERE status = 'ACTIVE' AND created_on > '2024-01-01'\n\n-- Index: (status, created_on) → seeks to 'ACTIVE', then escaneos rango\n```"
      },
      {
        "heading": "Índices de Cobertura (Index-Solo Scan)",
        "content": "If the índice contiene ALL columnas necesario by the consulta, the base de datos nunca lecturas the tabla:\n\n```sql\nCREATE INDEX idx_order_covering\nON orders(status, created_on)\nINCLUDE (total, customer_id);  -- PostgreSQL INCLUDE syntax\n\n-- This consulta is satisfied entirely desde the índice:\nSELECT total, customer_id\nFROM orders\nWHERE status = 'SHIPPED' AND created_on > '2024-06-01';\n-- Index-only escaneo: no heap access necesario!\n```\n\nCovering índices trade disco space for leer speed. Best for frequently-run analytical consultas."
      },
      {
        "heading": "EXPLAIN ANALYZE — Interpretación de Planes de Ejecución",
        "content": "```sql\nEXPLAIN ANALYZE\nSELECT * FROM orders WHERE customer_id = 42 AND status = 'ACTIVE';\n```\n\nAspectos clave que hay que revisar:\n\n| Node | Meaning | Performance |\n|------|---------|:-----------:|\n| Seq Scan | Escaneo completo de la tabla | 🔴 Bad (on grande tablas) |\n| Index Scan | B-Tree búsqueda + heap fetch | 🟢 Good |\n| Index Solo Scan | Answered desde índice alone | 🟢 Best |\n| Bitmap Index Scan | Index → bitmap → heap | 🟡 OK (muchos filas) |\n| Nested Loop | For each outer fila, escaneo inner | 🔴 Bad if inner has no índice |\n| Hash Join | Build hash tabla, probe | 🟢 Adecuado para grande joins |\n\n**Tiempo real frente al estimado:** Always use `ANALYZE` to see real execution times. Estimates puede be wildly wrong if statistics are stale (`ANALYZE table_name` to refresh)."
      },
      {
        "heading": "Índices Parciales y de Expresión",
        "content": "**Índice parcial:** Solo índice filas that match a condición:\n```sql\n-- Solo índice active orders (skip 90% of historical datos)\nCREATE INDEX idx_active_orders ON orders(created_on)\nWHERE status = 'ACTIVE';\n```\nMuch smaller índice → más rápido búsquedas, menos disco, menos maintenance.\n\n**Índice de expresión:** Index a computed valor:\n```sql\n-- Case-insensitive search\nCREATE INDEX idx_email_lower ON users(LOWER(email));\nSELECT * FROM users WHERE LOWER(email) = 'user@example.com';\n\n-- JSONB field\nCREATE INDEX idx_meta_type ON products((metadata->>'type'));\n```"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-indexing"
  },
  {
    "id": "theory-dp-transactions",
    "title": "Transacciones y ACID — Corrección bajo Concurrencia",
    "sections": [
      {
        "heading": "Propiedades ACID",
        "content": "| Property | Meaning | Mechanism |\n|----------|---------|----------|\n| **Atomicity** | Todo o nada: un fallo parcial provoca una reversión | Undo log (rollback segment) |\n| **Consistency** | La base de datos pasa de un estado válido a otro | Constraints, triggers, app logic |\n| **Isolation** | Las transacciones concurrentes no interfieren entre sí | MVCC + bloqueos |\n| **Durability** | Los datos confirmados sobreviven a fallos | WAL (Write-Ahead Log) |\n\n**En la práctica:** Atomicity and Durability are handled by the base de datos motor. Consistency is mostly your responsibility (correcto constraints + aplicación logic). Isolation is donde 90% of bugs hide."
      },
      {
        "heading": "Niveles de Aislamiento en la Práctica",
        "content": "| Level | Dirty Read | Non-Repeatable Read | Phantom | Performance |\n|-------|:----------:|:-------------------:|:-------:|:-----------:|\n| READ UNCOMMITTED | ✅ allowed | ✅ allowed | ✅ allowed | Fastest |\n| READ COMMITTED | ❌ blocked | ✅ allowed | ✅ allowed | Default (PG, Oracle) |\n| REPEATABLE READ | ❌ blocked | ❌ blocked | ✅ allowed* | Moderate |\n| SERIALIZABLE | ❌ blocked | ❌ blocked | ❌ blocked | Slowest |\n\n*PostgreSQL's REPEATABLE READ actually prevents phantoms (true instantánea aislamiento).\n\n**Valor predeterminado en PostgreSQL:** READ COMMITTED — each SQL statement sees a instantánea nueva of committed datos.\n**Valor predeterminado en MySQL InnoDB:** REPEATABLE READ — entire transacción sees one instantánea."
      },
      {
        "heading": "Bloqueos — Pesimista vs Optimista",
        "content": "**Bloqueo pesimista** — block other transacciones:\n```sql\n-- Exclusive bloqueo: blocks all other SELECTs FOR UPDATE and UPDATEs\nSELECT * FROM account WHERE id = 1 FOR UPDATE;\nUPDATE account SET balance = balance - 100 WHERE id = 1;\nCOMMIT;\n```\n\n**Bloqueo optimista** — detect conflictos después the fact:\n```sql\n-- Read version\nSELECT id, balance, version FROM account WHERE id = 1;\n-- (balance=500, version=3)\n\n-- Update only if version matches\nUPDATE account SET balance = 400, version = 4\nWHERE id = 1 AND version = 3;\n-- If rows_affected = 0 → conflicto detected → reintentar or fail\n```\n\n**Cuándo utilizar cada uno:**\n- Web app con bajo conflicto → Optimistic (JPA @Version)\n- Financial transfer, inventory decrement → Pessimistic (SELECT FOR UPDATE)\n- Batch job modifying muchos filas → Advisory bloqueos or queue-based"
      },
      {
        "heading": "Interbloqueos",
        "content": "Deadlock: Transaction A holds bloqueo on fila 1, waits for fila 2. Transaction B holds bloqueo on fila 2, waits for fila 1. Neither puede proceed.\n\n```\nT1: UPDATE account SET ... WHERE id = 1;  -- bloqueos fila 1\nT2: UPDATE account SET ... WHERE id = 2;  -- bloqueos fila 2\nT1: UPDATE account SET ... WHERE id = 2;  -- WAITS for T2\nT2: UPDATE account SET ... WHERE id = 1;  -- WAITS for T1 → DEADLOCK!\n```\n\n**Prevention:**\n1. Always acquire bloqueos in the **mismo orden** (e.g., by ascending ID)\n2. Keep transacciones **short** (menos time holding bloqueos)\n3. Use **bloqueo timeouts** (`SET lock_timeout = '5s'`)\n4. Design con **queues** en su lugar of competing for the mismo filas\n\nPostgreSQL automáticamente detects deadlocks and kills one transacción con an error. Your app debe be ready to reintentar."
      },
      {
        "heading": "Patrones de Transacciones en Spring/JPA",
        "content": "```java\n// Default: REQUIRED — join existing TX or create nuevo one\n@Transactional\npublic void transfer(Long desde, Long to, BigDecimal amount) {\n    Account sender = accountRepo.findByIdForUpdate(desde); // pessimistic\n    Account receiver = accountRepo.findById(to).orElseThrow();\n    sender.debit(amount);\n    receiver.credit(amount);\n    // TX commits on method exit — both accounts updated atomically\n}\n\n// REQUIRES_NEW — siempre nuevo TX (útil for audit logging)\n@Transactional(propagation = Propagation.REQUIRES_NEW)\npublic void auditLog(String action) {\n    // Committed even if caller transacción rolls back\n}\n```\n\n**Error habitual:** Calling `@Transactional` methods desde within the misma clase. Spring proxies only intercept calls desde fuera del bean — la autoinvocación evita el proxy."
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-transactions"
  },
  {
    "id": "theory-dp-performance",
    "title": "Rendimiento SQL — El Problema N+1 y la Optimización",
    "sections": [
      {
        "heading": "El problema de consultas N+1",
        "content": "The most habitual rendimiento issue in web applications using ORM:\n\n<pre><code>// ❌ N+1 problema: 1 consulta for deliveries + 1 consulta PER delivery for stops\nList&lt;Delivery&gt; deliveries = repo.findAll(); // 1 consulta: SELECT * FROM delivery\nfor (Delivery d : deliveries) {\n    d.getStops().size(); // N consultas: SELECT * FROM stop WHERE delivery_id = ?\n}\n// 100 deliveries = 101 consultas!</code></pre>\n\n<strong>Solutions:</strong>\n<pre><code>// 1. JOIN FETCH (JPQL) — single consulta con JOIN\n@Query(\"SELECT d FROM Delivery d JOIN FETCH d.stops WHERE d.status = :status\")\nList&lt;Delivery&gt; findWithStops(@Param(\"status\") DeliveryStatus status);\n\n// 2. @EntityGraph — declarative eager carga\n@EntityGraph(attributePaths = {\"stops\", \"driver\"})\nList&lt;Delivery&gt; findByStatus(DeliveryStatus status);\n\n// 3. Batch carga (Hibernate) — reduces N+1 to N/lote+1\n@BatchSize(size = 25)  // loads stops in batches of 25\nprivate List&lt;Stop&gt; stops;</code></pre>\n\n<strong>Detection:</strong> Enable SQL logging in dev, count consultas por solicitud. If you see the mismo consulta repeated con diferente IDs → N+1."
      },
      {
        "heading": "Lista de comprobación para optimizar consultas",
        "content": "<strong>Optimize by evidence, not by folklore.</strong> The útil order is: measure the lento solicitud, inspect its plan, make one change, then measure again con representative datos. This is más reliable than adding an índice or rewriting a consulta porque it \"looks más rápido\".\n\n<strong>1. Return and process menos datos:</strong>\n<pre><code>-- ❌ Reads wide filas and transfers columnas the screen nunca uses\nSELECT * FROM delivery WHERE status = 'PLANNED';\n\n-- ✓ Small projection, stable order and a bounded result\nSELECT id, status, driver_id, scheduled_at\nFROM delivery\nWHERE status = 'PLANNED'\nORDER BY scheduled_at, id\nLIMIT 50;</code></pre>\nUse a DTO projection in JPA cuando the screen is leer-only. `LIMIT` is not a substitute for pagination, but cada user-facing list needs a deliberate bound.\n\n<strong>2. Keep predicates sargable:</strong> an indexed columna debe be searchable sin applying a function or implicit cast to cada fila.\n<pre><code>-- ❌ Function wraps the indexed columna\nWHERE EXTRACT(YEAR FROM scheduled_at) = 2026\n\n-- ✓ Range predicate puede use an índice on scheduled_at\nWHERE scheduled_at >= TIMESTAMPTZ '2026-01-01'\n  AND scheduled_at <  TIMESTAMPTZ '2027-01-01'</code></pre>\nThe mismo principle applies to `LOWER(email)`, leading-wildcard `LIKE '%term'`, mismatched datos types and arithmetic on a columna. If the expression is genuinely required, consider an expression índice — después measuring.\n\n<strong>3. Design an índice for the access path:</strong> start desde the frequent `WHERE`, `JOIN`, `ORDER BY` and selected columnas together. For a list filtered by status and ordered by date, `(status, scheduled_at, id)` is a plausible candidate; its leftmost columnas matter. Indexing cada columna or cada bajo-cardinality valor simply makes escrituras and maintenance más costoso.\n\n<strong>4. Treat EXISTS, IN, JOIN and CTEs as semantic choices primero:</strong> `EXISTS` expresses \"does a related fila exist?\" and avoids duplicate parent filas. `JOIN` is derecha cuando columnas desde both relations are necesario. PostgreSQL puede optimize `IN` and a non-materialized CTE well, so do not assume one is siempre más rápido — inspect the plan. A CTE is often a readability herramienta; `MATERIALIZED` deliberately changes that trade-off."
      },
      {
        "heading": "Lee el plan antes de cambiar el SQL",
        "content": "<pre><code>EXPLAIN (ANALYZE, BUFFERS)\nSELECT d.id, d.status, d.scheduled_at\nFROM delivery d\nWHERE d.status = 'PLANNED'\n  AND d.scheduled_at >= now() - interval '7 days'\nORDER BY d.scheduled_at, d.id\nLIMIT 50;</code></pre>\n\n<strong>Read a PostgreSQL plan in this order:</strong>\n• <strong>Actual time and loops:</strong> buscar the nodo doing the real work, not merely the nodo at the top. A cheap operation repeated thousands of times is costoso.\n• <strong>Estimated filas vs real filas:</strong> a grande difference normalmente points to estadísticas desactualizadas, correlated columnas or a poor predicate estimate. Run `ANALYZE` donde appropriate; only then consider extended statistics or a consulta/índice change.\n• <strong>Access path:</strong> a sequential escaneo is not automáticamente malo — it is often correcto for a pequeño tabla or a consulta returning most filas. It becomes suspicious cuando it lecturas a grande tabla to devolver a tiny fraction.\n• <strong>Sort, hash and temporary I/O:</strong> external disco sorts, grande hash batches and repeated nested-loop inner escaneos are concrete leads. Check filters, join índices and fila counts antes forcing a plan.\n• <strong>Buffers:</strong> shared hits versus lecturas tell you whether the issue is CPU/planning or almacenamiento I/O.\n\n`EXPLAIN ANALYZE` executes the statement. Use it carefully con escrituras and producción datos; prefer a transacción that you puede roll back for investigative DML."
      },
      {
        "heading": "JOINs, paginación y trabajo repetido",
        "content": "<strong>Joins:</strong> índice join claves on the grande/inner side, filter early cuando it reduces filas, and project only qué is necesario. Do not manually reorder joins to outsmart PostgreSQL sin evidence; the planner puede reorder muchos joins, while estadísticas desactualizadas puede make it elegir poorly.\n\n<strong>Pagination:</strong> paginación por desplazamiento is sencillo and supports jumping to a página, but later offsets still require the base de datos to buscar and discard earlier filas. Cursor/paginación por clave is normalmente the better predeterminado for a \"next página\" feed.\n<pre><code>-- Keyset pagination: stable, bounded work por next página\nSELECT id, status, scheduled_at\nFROM delivery\nWHERE (scheduled_at, id) > (:lastScheduledAt, :lastId)\nORDER BY scheduled_at, id\nLIMIT 50;</code></pre>\nThe ordering debe be deterministic and supported by an índice such as `(scheduled_at, id)`.\n\n<strong>Sorting and aggregation:</strong> `ORDER BY`, `GROUP BY`, `DISTINCT` and `UNION` puede require substantial memoria, disco or deduplication work. Remove them cuando the product does not necesitar them; use `UNION ALL` only cuando duplicates are valid. For costoso, repeated reporting aggregates, evaluate a materialized view or a pre-aggregation pipeline and define its refresh/freshness contract.\n\n<strong>Production guardrails:</strong> set a sensible statement timeout, observe lento-consulta metrics, keep statistics real, and verify the improvement at producción-like cardinality. An índice that speeds up a leer puede increase latencia de escritura, vacuum work and almacenamiento — that trade-off belongs in the decision."
      },
      {
        "heading": "Perspectiva Sénior",
        "content": "Senior engineers don't just escritura SQL — they leer plan de ejecucións, understand the planificador de consultas's decisions, and design schemas for their access patterns.\n\nKey distinctions:\n• An índice that improves one consulta puede degrade escrituras entre the tabla\n• EXPLAIN ANALYZE is the single most importante debugging herramienta\n• The difference entre a 100ms consulta and a 10s consulta is often one missing composite índice\n• Isolation levels are a trade-off entre correctness and throughput, not a predeterminado to set and forget\n• Connection pooling, statement timeouts and bloqueo monitoring are producción essentials"
      },
      {
        "heading": "Perspectiva REWE",
        "content": "In TRAB's delivery base de datos:\n\n• Route planning consultas join deliveries × stops × drivers × time windows\n• Indexes debe support both real-time búsquedas (by driver, by route) and lote reporting (by date rango, by warehouse)\n• PostgreSQL's MVCC means long-running reports don't block transactional inserts — but they puede bloat the tabla\n• Partitioning delivery_events by month keeps recent datos rápido while archiving history\n• The N+1 problema appears cuando carga route details for a list of deliveries — solved con JOIN FETCH or lote carga"
      },
      {
        "heading": "Cuándo NO utilizarlo",
        "content": "When NOT to optimize SQL prematurely:\n\n• Don't add índices antes measuring — each índice costs escritura rendimiento and almacenamiento\n• Don't denormalize antes proving the JOIN is actually the bottleneck\n• No lo utilices base de datos-level caching cuando aplicación-level caching (Redis) is más appropriate\n• No lo utilices SERIALIZABLE aislamiento unless you puede prove the anomaly you're preventing\n• No lo utilices stored procedures for business logic that changes frequently"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-query-optimization"
  },
  {
    "id": "theory-dp-storage-engines",
    "title": "Motores de Almacenamiento — B-Trees vs LSM-Trees (DDIA Cap. 3)",
    "sections": [
      {
        "heading": "Por qué los motores de almacenamiento importan",
        "content": "You won't implement a almacenamiento motor, but you <strong>debe</strong> elegir one appropriate for your workload. The two dominant families:\n\n• <strong>B-Trees</strong> (PostgreSQL, MySQL InnoDB, Oracle) — página-oriented, in-place updates\n• <strong>LSM-Trees</strong> (Cassandra, RocksDB, LevelDB, HBase) — log-structured, solo anexado, compaction\n\nThe choice affects leer/escritura rendimiento, amplificación de espacio, and compaction overhead. Understanding the internals lets you predict behavior bajo load and tune accordingly.\n\n<strong>Idea clave desde Kleppmann:</strong> \"Well-chosen índices speed up leer consultas, but cada índice slows down escrituras. Databases don't índice everything by predeterminado — you elegir índices using your knowledge of the aplicación's consulta patterns.\""
      },
      {
        "heading": "B-Trees — la base de las bases de datos relacionales",
        "content": "<strong>Cómo funcionan los B-Trees:</strong>\n• Data organized in fixed-size <strong>páginas</strong> (normalmente 4KB)\n• Each página puede be identified by an address (like a pointer on disco)\n• Pages form a árbol: raíz → internal nodos → hoja nodos containing real valores\n• Lookups traverse desde raíz to hoja: O(log n) — normalmente 3-4 levels for millions of filas\n• <strong>Branching factor</strong> ~500: a 4-level árbol puede store up to 256 TB\n\n<pre><code>-- B-árbol búsqueda for: WHERE id = 42\nRoot página → \"id entre 1-100 is in página 3\"\n  Page 3 → \"id entre 40-60 is in página 17\"\n    Page 17 → found fila con id=42 (hoja página)\n-- Solo 3 disco lecturas for millions of filas!</code></pre>\n\n<strong>Write operations:</strong>\n• Buscar the hoja página containing the clave\n• Overwrite the página in place con the nuevo valor\n• If página is full → split en two half-full páginas, update parent\n• Uses <strong>Write-Ahead Log (WAL)</strong> for crash recovery — cada modification written to solo anexado log BEFORE updating árbol páginas\n\n<strong>Ventajas:</strong>\n• Excellent rendimiento de lectura (O(log n))\n• Predictable latency\n• Efficient consultas por rango (hoja páginas linked)\n• Mature, well-understood, decades of optimization\n\n<strong>Desventajas:</strong>\n• Write amplification: one logical escritura = WAL escritura + página escritura (possibly + página split)\n• Fragmentation sobre time\n• Concurrent access requires careful locking (latches on páginas)"
      },
      {
        "heading": "LSM-Trees — optimizados para cargas de escritura",
        "content": "<strong>La idea principal:</strong> nunca modify datos in place. Todas escrituras go to an in-memoria balanced árbol (memtable). When memtable is full, flush to disco as a ordenado file (SSTable).\n\n<strong>SSTable (Sorted String Table):</strong>\n• Key-valor pairs ordenado by clave\n• Immutable once written\n• Merging is efficient (merge-sort of ordenado files)\n• Sparse índice in memoria (one entry por pocos KB)\n\n<pre><code>Ruta de escritura:\n1. Write to in-memoria memtable (Red-Black árbol / AVL)\n2. When memtable reaches threshold (~MB) → flush to disco as SSTable\n3. Background compaction: merge SSTables, discard antiguo versions\n\nRuta de lectura:\n1. Check memtable\n2. Check most recent SSTable on disco\n3. Check next SSTable... (use Bloom filters to skip)\n</code></pre>\n\n<strong>Estrategias de compactación:</strong>\n• <strong>Size-tiered</strong> (Cassandra predeterminado): merge SSTables of similar size → bueno rendimiento de escritura, higher amplificación de espacio\n• <strong>Leveled</strong> (RocksDB, LevelDB): organized in levels of increasing size → better rendimiento de lectura, más compaction I/O\n\n<strong>Ventajas:</strong>\n• Sequential escrituras only → much más rápido on HDD and SSD (10x-100x rendimiento de escritura vs B-árbol)\n• Better compression (ordenado datos compresses well)\n• No fragmentation (compaction produces contiguous files)\n\n<strong>Desventajas:</strong>\n• Reads slower (debe check varias SSTables)\n• Compaction puede interfere con ongoing lecturas/escrituras (I/O bandwidth)\n• Unpredictable latencia de cola durante compaction\n• Space amplification: antiguo versions exist until compacted away"
      },
      {
        "heading": "Matriz de decisión: cuándo elegir cada uno",
        "content": "<strong>Elige B-Tree (PostgreSQL, MySQL) cuando:</strong>\n• Read-heavy workload (OLTP con muchos búsquedas)\n• Need predictable latency (p99 matters)\n• Range consultas are habitual\n• Transactions con aislamiento fuerte required\n• Data fits in memoria or conjunto de trabajo is hot\n\n<strong>Elige LSM-Tree (Cassandra, RocksDB) cuando:</strong>\n• Write-heavy workload (event logging, time-series, IoT)\n• Write throughput is the bottleneck\n• Can tolerate slightly higher latencia de lectura\n• Data is append-mostly (logs, events, metrics)\n• Need better space efficiency\n\n<strong>Para un sistema de entregas de REWE:</strong>\n• <strong>Base de datos OLTP principal</strong> (orders, drivers, routes): B-Tree (PostgreSQL) — lecturas dominate, transacciones crítico\n• <strong>Almacén de eventos</strong> (GPS pings, delivery events, telemetry): LSM-Tree — escritura-heavy, solo anexado, consistencia eventual OK\n• <strong>Índice de búsqueda</strong> (Elasticsearch uses LSM segments internamente)\n\n<strong>Idea clave para la entrevista:</strong> \"The choice entre B-Tree and LSM isn't about which is 'better' — it's about coincidentes the almacenamiento motor to your access pattern. B-Trees optimize for lecturas; LSM-Trees optimize for escrituras. Most producción systems use both for diferente datos.\""
      },
      {
        "heading": "OLTP vs OLAP — procesamiento transaccional frente a analítica",
        "content": "DDIA makes a crucial distinction entre two almacenamiento patterns:\n\n<pre><code>                OLTP (fila-oriented)          OLAP (columna-oriented)\nRead pattern    Small number of registros       Aggregate sobre muchos filas\n                by clave (búsquedas puntuales)        (escaneo grande datasets)\nWrite pattern   Random-access, bajo-latency    Bulk import (ETL) or event\n                user-driven inserts           streaming\nUsed by         End users via web app         Internal analysts, BI tools\nData size       GB to bajo TB                  TB to PB\nBottleneck      Disk seek time                Disk bandwidth\nExample         PostgreSQL, MySQL              BigQuery, Redshift, Clickhouse\n</code></pre>\n\n<strong>Almacenamiento orientado a columnas (OLAP):</strong>\n• Stores all valores of a columna together (not all columnas of a fila)\n• Enables incredible compression (run-length encoding, bitmap encoding)\n• Reads only the columnas necesario for the consulta\n• Vectorized processing: CPU operates on compressed columna vectors\n\n<pre><code>-- This consulta in columna store lecturas ONLY status and scheduled_at columnas:\nSELECT status, COUNT(*) FROM delivery\nWHERE scheduled_at >= '2026-01-01'\nGROUP BY status;\n-- In fila store: lecturas ENTIRE filas (40 columnas) just to use 2</code></pre>\n\n<strong>Vistas materializadas y cubos de datos:</strong>\nPre-computed aggregates for habitual consultas. Trade almacenamiento and escritura cost for instant lecturas. DDIA calls this \"aggregation: datos cubes and materialized views.\""
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-storage-engines"
  },
  {
    "id": "theory-dp-replication",
    "title": "Replicación — Mantener los Datos Consistentes entre Máquinas",
    "sections": [
      {
        "heading": "¿Por qué replicar?",
        "content": "Tres motivos para mantener copias de los datos en varias máquinas:\n\n1. <strong>Alta disponibilidad</strong> — if one nodo dies, others continue serving\n2. <strong>Latency</strong> — serve users desde geographically close réplicas\n3. <strong>Escalabilidad de lectura</strong> — distribute leer load entre réplicas\n\nAll difficulty in replication lies in <strong>handling changes</strong>. If datos nunca changed, you'd copy it once and be done. The challenge is keeping réplicas consistente as escrituras happen.\n\n<strong>Tres enfoques principales:</strong>\n• Basado en líder (líder único)\n• Multilíder\n• Sin líder"
      },
      {
        "heading": "Replicación Líder-Seguidor (un solo líder)",
        "content": "El enfoque más habitual (PostgreSQL, MySQL, Oracle, MongoDB):\n\n<pre><code>┌─────────┐    escrituras    ┌─────────┐\n│  Client │────────────→│  Leader │\n└─────────┘             └────┬────┘\n                             │ replication stream\n                    ┌────────┼────────┐\n                    ▼        ▼        ▼\n              ┌──────┐ ┌──────┐ ┌──────┐\n              │Foll.1│ │Foll.2│ │Foll.3│\n              └──────┘ └──────┘ └──────┘\n                  ▲        ▲        ▲\n              ┌───┴────────┴────────┴───┐\n              │    Clients leer desde     │\n              │    líder OR seguidores   │\n              └─────────────────────────┘</code></pre>\n\n<strong>Rules:</strong>\n• Solo líder accepts escrituras\n• Followers receive replication log and apply changes in mismo orden\n• Reads puede go to any réplica (líder or seguidor)\n\n<strong>Replicación síncrona frente a asíncrona:</strong>\n• <strong>Síncrona</strong>: líder waits for seguidor to confirm → guaranteed up-to-date, but any seguidor outage blocks escrituras\n• <strong>Asíncrona</strong>: líder doesn't wait → rápido escrituras, but seguidor puede lag (datos loss if líder crashes)\n• <strong>Semisíncrona</strong> (practical): one seguidor sync + others async\n\n<strong>PostgreSQL streaming replication:</strong>\n<pre><code>-- Primary sends WAL registros to standby in real-time\n-- Standby applies WAL and puede serve leer consultas\n-- Typical lag: milliseconds to seconds\n-- pg_stat_replication shows lag por réplica</code></pre>"
      },
      {
        "heading": "Retraso de replicación — problemas de consistencia",
        "content": "When using async replication, seguidores puede be seconds behind the líder. This creates observable anomalies:\n\n<strong>1. Lectura de tus propias escrituras (leer-después-escritura consistency):</strong>\n<pre><code>-- User updates their profile (goes to líder)\nUPDATE user_profile SET name = 'Alice' WHERE id = 42;\n\n-- User inmediatamente refreshes página (lecturas desde seguidor)\nSELECT name FROM user_profile WHERE id = 42;\n-- May still devolver antiguo name if seguidor hasn't caught up!</code></pre>\n\n<strong>Solutions:</strong>\n• Read user's own datos desde líder\n• Track escritura timestamp, route lecturas to réplica that's caught up\n• Client remembers its last escritura timestamp\n\n<strong>2. Lecturas monotónicas:</strong>\nUser sees datos, refreshes, sees OLDER datos (diferente seguidor, further behind).\nSolution: pin user to one réplica (session affinity).\n\n<strong>3. Lecturas con prefijo consistente:</strong>\nCausal order violated — seeing an answer antes the question.\nSolution: escritura causally related datos to mismo partición.\n\n<strong>Interview answer framework:</strong> \"Replication lag is an unavoidable consequence of asynchronous replication. The aplicación debe be designed to tolerate it — either by enrutamiento specific lecturas to the líder, or by building the UI to handle consistencia eventual gracefully.\""
      },
      {
        "heading": "Replicación Multi-Líder",
        "content": "<strong>When:</strong> Multiple datacenters, each con its own líder. Writes accepted at any datacenter.\n\n<pre><code>DC Europe          DC US-East         DC Asia\n┌─────────┐       ┌─────────┐       ┌─────────┐\n│ Leader  │◄─────►│ Leader  │◄─────►│ Leader  │\n│+Followers│      │+Followers│      │+Followers│\n└─────────┘       └─────────┘       └─────────┘\n   escrituras↑           escrituras↑           escrituras↑</code></pre>\n\n<strong>Ventajas:</strong>\n• Tolerates entire datacenter outage\n• Lower latencia de escritura (local líder)\n• Network interruptions entre DCs don't block local escrituras\n\n<strong>The BIG problema: escritura conflictos</strong>\n<pre><code>-- User edits title simultáneamente in two DCs\nDC-Europe: UPDATE doc SET title = 'Title A' WHERE id = 1;\nDC-US:     UPDATE doc SET title = 'Title B' WHERE id = 1;\n-- Both succeed locally. Which one wins cuando they replicate?</code></pre>\n\n<strong>Estrategias de resolución de conflictos:</strong>\n• <strong>La última escritura gana (LWW)</strong>: use timestamp → sencillo but loses datos (Cassandra predeterminado)\n• <strong>Combinar valores</strong>: keep both and let aplicación resolve (CRDTs)\n• <strong>Lógica personalizada</strong>: aplicación-specific handlers\n• <strong>Prevención de conflictos</strong>: route all escrituras for a record to mismo líder\n\n<strong>Usos prácticos:</strong> collaborative editing (Google Docs), mobile offline-primero apps, cross-region deployment."
      },
      {
        "heading": "Replicación sin Líder (estilo Dynamo)",
        "content": "No líder at all. Client escrituras to varias réplicas directamente (Cassandra, Riak, DynamoDB).\n\n<pre><code>Write: send to ALL réplicas, succeed if W out of N acknowledge\nRead:  leer desde ALL réplicas, succeed if R out of N respond\n\nQuorum condición: W + R > N guarantees overlap\n-- With N=3, W=2, R=2: siempre leer at least one up-to-date copy</code></pre>\n\n<strong>Example con N=3, W=2, R=2:</strong>\n<pre><code>Client escrituras valor v2 to all 3 réplicas:\n  Replica A: ✓ (has v2)\n  Replica B: ✓ (has v2)  → W=2 met, escritura succeeds\n  Replica C: ✗ (still has v1, was temporarily down)\n\nClient lecturas desde all 3:\n  Replica A: devuelve v2\n  Replica B: devuelve v2  → R=2 met, cliente sees v2 ✓\n  Replica C: devuelve v1 (stale)</code></pre>\n\n<strong>Repair mechanisms:</strong>\n• <strong>Read repair</strong>: cliente detects stale valor durante leer, escrituras back to stale réplica\n• <strong>Anti-entropy</strong>: background process comparing réplicas and copying missing datos\n\n<strong>Limitations of quorums:</strong>\nEven con W + R > N, edge cases puede devolver stale lecturas:\n• Sloppy quorums (escrituras go to nodos outside the designated N)\n• Concurrent escrituras (which one wins?)\n• Write concurrent con leer (puede see antiguo or nuevo)\n• Partial failures durante escrituras\n\n<strong>Cuándo utilizarlo:</strong> Alta disponibilidad requirements, tolerate consistencia eventual, multi-datacenter sin a single líder. Not ideal for strong consistency needs (banking, inventory)."
      },
      {
        "heading": "Perspectiva sénior — elegir una estrategia de replicación",
        "content": "<strong>Decision framework for interview:</strong>\n\n| Requirement | Strategy |\n|---|---|\n| Strong consistency, single region | Single líder (PostgreSQL streaming) |\n| Alta disponibilidad, tolerate lag | Single líder + async seguidores |\n| Multi-region escrituras | Multilíder (accept conflicto complexity) |\n| Maximum availability, consistencia eventual OK | Sin líder (Cassandra/DynamoDB) |\n| Escalabilidad de lectura | Add leer réplicas to any estrategia |\n\n<strong>Key Kleppmann quotes for interviews:</strong>\n• \"If the líder fails and is not recoverable, any escrituras that have not yet been replicated to seguidores are lost.\"\n• \"Pretending that replication is synchronous cuando in fact it is asynchronous is a recipe for problems down the line.\"\n• \"There is no general solución to conflicto resolution... the aplicación developer debe resolve conflictos in a way that makes sense for the business logic.\"\n\n<strong>REWE aplicación:</strong>\n• Delivery tracking base de datos: líder único PostgreSQL con streaming replication (strong consistency for order estado)\n• Analytics/reporting: leer réplicas in each region\n• Driver GPS events: could use leaderless for maximum availability (stale position is better than no position)"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-replication"
  },
  {
    "id": "theory-dp-partitioning",
    "title": "Particionamiento y sharding — Escalar más allá de una sola máquina",
    "sections": [
      {
        "heading": "¿Por qué particionar los datos?",
        "content": "When datos grows too grande for one machine, split it en <strong>particiones</strong> (también called shards, regions, tablets, vnodes).\n\nEach partición is a pequeño base de datos of its own — lives on one nodo (con replication for fault tolerance).\n\n<pre><code>Before: 1 nodo holds 10 TB\nAfter:  10 nodos hold 1 TB each (partitioned)\n\n         ┌──────────────────────────────┐\n         │   Query Router / Client      │\n         └─────┬──────┬──────┬──────────┘\n               │      │      │\n          ┌────▼─┐ ┌──▼──┐ ┌─▼────┐\n          │Part A│ │Part B│ │Part C│\n          │A-H   │ │I-P   │ │Q-Z   │\n          └──────┘ └─────┘ └──────┘</code></pre>\n\n<strong>Goal:</strong> spread datos and consulta load evenly. Avoid <strong>hot spots</strong> (one partición getting disproportionate traffic).\n\nPartitioning is normalmente combined con replication: each partición has varias réplicas on diferente nodos."
      },
      {
        "heading": "Particionamiento por rango de claves",
        "content": "Assign a continuous rango of claves to each partición (like volumes of an encyclopedia).\n\n<pre><code>Partition 1: delivery_id 1 - 100,000\nPartition 2: delivery_id 100,001 - 200,000\nPartition 3: delivery_id 200,001 - 300,000\n\nOr by date:\nPartition Jan-2026: events donde timestamp in January\nPartition Feb-2026: events donde timestamp in February</code></pre>\n\n<strong>Ventajas:</strong>\n• Efficient rango escaneos (all datos for a rango is on one partición)\n• Easy to understand boundaries\n\n<strong>Desventajas:</strong>\n• <strong>Hot spots</strong>: if escrituras have a natural ordering (timestamps), all escrituras go to the LATEST partición while others sit idle\n• Need manual or automatic rebalancing as datos grows\n\n<strong>Mitigation for time-series hot spots:</strong>\n<pre><code>-- Instead of partitioning by timestamp alone:\n-- Prefix con sensor_id or region → spreads escrituras\nPartition clave = sensor_name + date\n-- sensor_A_2026-07-21, sensor_B_2026-07-21 → diferente particiones</code></pre>\n\n<strong>Used by:</strong> HBase, BigTable, RethinkDB, MongoDB (con ranged shard clave)"
      },
      {
        "heading": "Particionamiento por hash de la clave",
        "content": "Apply a hash function to the clave, assign hash ranges to particiones. Eliminates hot spots desde ordered claves.\n\n<pre><code>hash(clave) → 0 to 2^32\nPartition A: hash 0 - 1,073,741,823\nPartition B: hash 1,073,741,824 - 2,147,483,647\nPartition C: hash 2,147,483,648 - 3,221,225,471\nPartition D: hash 3,221,225,472 - 4,294,967,295</code></pre>\n\n<strong>Ventajas:</strong>\n• Even distribution regardless of clave patterns\n• No hot spots (even sequential claves get spread out)\n\n<strong>Desventajas:</strong>\n• <strong>Loses rango consulta efficiency</strong>: adjacent claves end up on diferente particiones\n• Range escaneos require querying ALL particiones (scatter/gather)\n\n<strong>Compromise: compound partición clave</strong> (Cassandra)\n<pre><code>-- First part hashed (for distribution), rest ordenado within partición\nPRIMARY KEY ((driver_id), delivery_date, delivery_id)\n-- Todas deliveries for a driver on mismo partición (hashed by driver_id)\n-- Within that partición, ordenado by date → efficient rango escaneo por driver</code></pre>\n\n<strong>Used by:</strong> Cassandra, DynamoDB, Riak, Voldemort"
      },
      {
        "heading": "Índices secundarios con particionamiento",
        "content": "Partitioning gets complicated cuando you necesitar secondary índices (not just búsqueda by primary clave).\n\n<strong>Approach 1: Local índice (document-partitioned)</strong>\nEach partición maintains its own secondary índice covering only its datos.\n\n<pre><code>Partition 0:                 Partition 1:\ndeliveries id 0-499         deliveries id 500-999\nlocal índice: status         local índice: status\n  PLANNED → [1,7,42]        PLANNED → [501,622]\n  DELIVERED → [3,9]          DELIVERED → [503,700]</code></pre>\n\n• Write: update only local índice (rápido)\n• Read by secondary índice: <strong>scatter/gather</strong> — consulta ALL particiones, merge results\n• Used by: MongoDB, Riak, Cassandra, Elasticsearch, Solr\n\n<strong>Approach 2: Global índice (term-partitioned)</strong>\nThe índice itself is partitioned — by the indexed term, not by document.\n\n<pre><code>Index partición A (status A-M):    Index partición B (status N-Z):\n  DELIVERED → [3,503,700]           PLANNED → [1,7,42,501,622]\n</code></pre>\n\n• Read: go to one índice partición (rápido lecturas)\n• Write: puede necesitar to update varias índice particiones (slower, often async)\n• Used by: DynamoDB global secondary índices, Riak search\n\n<strong>Idea clave para la entrevista:</strong> \"There's siempre a trade-off. Local índices make escrituras rápido but lecturas costoso. Global índices make lecturas rápido but escrituras require distributed coordination.\""
      },
      {
        "heading": "Reequilibrado — qué ocurre cuando se mueven las particiones",
        "content": "<strong>Triggers for rebalancing:</strong>\n• Add a nuevo nodo (scale out)\n• Remove a failed nodo\n• Data grows unevenly\n\n<strong>Strategies:</strong>\n\n<strong>1. Fixed number of particiones:</strong>\n• Create muchos más particiones than nodos (e.g., 1000 particiones for 10 nodos → 100 each)\n• When adding nodo: steal some particiones desde others\n• Partition boundaries don't change, only assignment to nodos\n• Used by: Riak, Elasticsearch, CouchBase, Voldemort\n\n<strong>2. Dynamic partitioning:</strong>\n• Split partición cuando it gets too grande, merge cuando too pequeño\n• Number of particiones adapts to datos volume\n• Used by: HBase, RethinkDB, MongoDB\n\n<strong>3. Proportional to nodos:</strong>\n• Fixed number of particiones por nodo\n• When nuevo nodo joins: randomly split existing particiones\n• Used by: Cassandra\n\n<strong>⚠️ Automatic vs manual rebalancing:</strong>\nKleppmann warns: fully automatic rebalancing puede be dangerous. If a nodo is lento (not dead), automatic rebalancing puede cause cascading failures. Many producción systems require human confirmation."
      },
      {
        "heading": "Enrutamiento de solicitudes — cómo encuentran los clientes la partición correcta",
        "content": "<pre><code>Three approaches:\n\n1. Client contacts ANY nodo → nodo forwards to correcto partición\n   [Client] → [Node 2] → [Node 5 has partición]\n\n2. Client contacts enrutamiento tier (partición-aware load balancer)\n   [Client] → [Router] → [Node 5 has partición]\n\n3. Client is partición-aware (knows the mapeo)\n   [Client] ─────────────→ [Node 5 has partición]</code></pre>\n\n<strong>How does anyone know which partición lives donde?</strong>\n• <strong>ZooKeeper/etcd</strong>: centralized metadata store (utilizado by HBase, Kafka, SolrCloud)\n• <strong>Gossip protocol</strong>: nodos share partición ownership among themselves (Cassandra)\n• <strong>Config file</strong>: manual partición mapeo (sencillo but static)\n\n<strong>PostgreSQL partitioning (built-in since v10):</strong>\n<pre><code>-- Declarative partitioning — no external coordinator necesario\nCREATE TABLE delivery_events (\n    id BIGINT,\n    event_time TIMESTAMPTZ,\n    event_type TEXT\n) PARTITION BY RANGE (event_time);\n\nCREATE TABLE events_2026_q1 PARTITION OF delivery_events\n    FOR VALUES FROM ('2026-01-01') TO ('2026-04-01');\nCREATE TABLE events_2026_q2 PARTITION OF delivery_events\n    FOR VALUES FROM ('2026-04-01') TO ('2026-07-01');\n-- Query planner automáticamente prunes irrelevant particiones</code></pre>\n\n<strong>REWE aplicación:</strong>\n• delivery_events: partitioned by month (most consultas have date rango)\n• GPS telemetry: partitioned by driver_id hash (even distribution)\n• Historical orders: partición by year, mover antiguo particiones to cold almacenamiento"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-partitioning"
  },
  {
    "id": "theory-dp-isolation-deep",
    "title": "Análisis profundo del aislamiento de transacciones — MVCC, escritura skew y serialización",
    "sections": [
      {
        "heading": "Más allá de los libros: qué revela DDIA sobre el aislamiento",
        "content": "The standard aislamiento levels (READ COMMITTED, REPEATABLE READ, SERIALIZABLE) hide enormous complexity. DDIA exposes qué really happens:\n\n<strong>Idea clave:</strong> \"There are big differences in the guarantees that databases actually provide, despite being ostensibly standardized. Nobody really knows qué 'repeatable leer' means.\"\n\n<strong>What each level actually prevents:</strong>\n\n| Anomaly | Read Committed | Snapshot/RR | Serializable |\n|---|---|---|---|\n| Dirty lecturas | ✓ prevented | ✓ | ✓ |\n| Dirty escrituras | ✓ prevented | ✓ | ✓ |\n| Read skew | ✗ possible | ✓ prevented | ✓ |\n| Lost updates | ✗ possible | ⚠️ depends on DB | ✓ |\n| Write skew | ✗ possible | ✗ possible | ✓ prevented |\n| Phantoms | ✗ possible | ✗ possible | ✓ prevented |\n\n⚠️ <strong>MySQL/InnoDB does NOT detect lost updates</strong> bajo repeatable leer. PostgreSQL does. This matters in producción."
      },
      {
        "heading": "MVCC — cómo funciona realmente el aislamiento por instantánea",
        "content": "Multi-Version Concurrency Control — the mechanism behind PostgreSQL's REPEATABLE READ:\n\n<pre><code>Key principle: readers nunca block writers, writers nunca block readers.\n\nImplementation (PostgreSQL):\n• Each transacción gets a unique, increasing txid\n• Each fila has created_by (txid) and deleted_by (txid) fields\n• UPDATE = mark antiguo fila deleted + insert nuevo fila\n• Each transacción only sees filas that were:\n  1. Created by an already-committed transacción (txid < mine)\n  2. NOT deleted, or deleted by a not-yet-committed transacción</code></pre>\n\n<strong>Visibility rules (the 4 condiciones desde DDIA):</strong>\n1. Ignore escrituras desde transacciones still in progress cuando mine started\n2. Ignore escrituras desde aborted transacciones\n3. Ignore escrituras desde transacciones con higher txid (started después me)\n4. Todas other escrituras are visible\n\n<pre><code>-- Practical ejemplo:\n-- TX 12 starts, sees instantánea at that moment\n-- TX 13 transfers $100 desde account A to B\n-- TX 12 still sees the OLD balances (consistente instantánea)\n-- Even después TX 13 commits, TX 12's instantánea doesn't change!</code></pre>\n\n<strong>Consequences:</strong>\n• Long-running consultas see a consistente point-in-time view\n• VACUUM debe clean up antiguo fila versions (dead tuples)\n• Table bloat if VACUUM puede't keep up con updates"
      },
      {
        "heading": "Write skew — la anomalía que el aislamiento por instantánea no evita",
        "content": "The most dangerous anomaly that muchos developers don't know about:\n\n<pre><code>-- Hospital on-call ejemplo (desde DDIA):\n-- Rule: at least 1 doctor debe be on-call at all times\n-- Currently: Alice and Bob are both on-call\n\n-- TX1 (Alice): \"Am I the only one on-call? No, Bob is too. I puede leave.\"\nSELECT COUNT(*) FROM doctors WHERE on_call = true AND shift_id = 123;\n-- Returns 2 → safe to remove myself\nUPDATE doctors SET on_call = false WHERE id = 'alice' AND shift_id = 123;\n\n-- TX2 (Bob): runs the SAME logic at the mismo time\nSELECT COUNT(*) FROM doctors WHERE on_call = true AND shift_id = 123;\n-- Also devuelve 2 (instantánea!) → safe to remove myself\nUPDATE doctors SET on_call = false WHERE id = 'bob' AND shift_id = 123;\n\n-- BOTH commit successfully. Now ZERO doctors are on-call! 💀</code></pre>\n\n<strong>Pattern:</strong>\n1. SELECT checks a condición\n2. Based on result, aplicación decides to proceed\n3. UPDATE/INSERT changes something that would invalidate the condición\n4. Another transacción does the mismo on a DIFFERENT fila\n\n<strong>More ejemplos of escritura skew:</strong>\n• Booking a meeting room (double-booking)\n• Claiming a unique username (two users register mismo name)\n• Preventing overdraft (two withdrawals check mismo balance)\n• Seat reservation on a flight\n\n<strong>Why instantánea aislamiento doesn't help:</strong>\nEach transacción lecturas a consistente instantánea and modifies DIFFERENT filas. No conflicto is detected porque they don't escritura to the mismo fila."
      },
      {
        "heading": "Fantasmas — el problema oculto",
        "content": "Write skew involves a <strong>phantom</strong>: a fila that one transacción's leer would have seen if another transacción's escritura had happened primero.\n\n<pre><code>-- Phantom ejemplo:\n-- TX1: SELECT * FROM bookings WHERE room = 'A' AND time = '14:00';\n-- Returns 0 filas → room is free → INSERT booking\n\n-- TX2: Same consulta, mismo time, también devuelve 0 → INSERT booking\n-- Result: DOUBLE BOOKING (phantom fila didn't exist cuando checked)</code></pre>\n\n<strong>The problema:</strong> You puede't use SELECT FOR UPDATE porque there's nothing to bloqueo — the fila doesn't exist yet!\n\n<strong>Solutions:</strong>\n\n<strong>1. Materializing conflictos:</strong>\n<pre><code>-- Create a tabla of time slots explicitly\nCREATE TABLE room_slots (\n    room_id INT, time_slot TIMESTAMPTZ, PRIMARY KEY (room_id, time_slot)\n);\n-- Pre-populate all possible slots\n-- Now you CAN bloqueo a fila:\nSELECT * FROM room_slots\nWHERE room_id = 'A' AND time_slot = '14:00'\nFOR UPDATE;\n-- If another TX tries to bloqueo the mismo slot → blocks</code></pre>\n\n<strong>2. Use SERIALIZABLE aislamiento:</strong>\n<pre><code>SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;\n-- PostgreSQL will detect the conflicto and abort one transacción\n-- Application debe reintentar the aborted transacción</code></pre>\n\n<strong>3. Application-level constraints:</strong>\nUNIQUE constraints, advisory bloqueos, explicit coordination."
      },
      {
        "heading": "Aislamiento serializable — tres implementaciones",
        "content": "The strongest aislamiento level. Three approaches exist:\n\n<strong>1. Actual Serial Execution (single-threaded)</strong>\n• Execute ALL transacciones one at a time, on a single CPU core\n• Surprisingly viable if: transacciones are short, datos fits in RAM\n• Used by: VoltDB, Redis, Datomic\n• Limitation: throughput limited to one core\n\n<strong>2. Two-Phase Locking (2PL)</strong>\n• Locks for lecturas AND escrituras. Readers block writers, writers block readers.\n• \"If TX A leer something, TX B puede't escritura it (until A commits)\"\n• Prevents ALL anomalies but massively reduces concurrency\n• Used by: MySQL/InnoDB SERIALIZABLE, SQL Server (partially), DB2\n• Problem: deadlocks, rendimiento degradation (up to 100x slower)\n\n<pre><code>-- 2PL in action:\nTX1: SELECT * FROM accounts WHERE id = 1; -- acquires shared bloqueo\nTX2: UPDATE accounts SET balance = 900 WHERE id = 1;\n     -- BLOCKED until TX1 commits/aborts!\n-- Readers block writers → significant rendimiento impact</code></pre>\n\n<strong>3. Serializable Snapshot Isolation (SSI)</strong>\n• Optimistic: transacciones execute sin blocking\n• At commit time: check if any conflictos occurred, abort if so\n• Used by: <strong>PostgreSQL SERIALIZABLE</strong> (since 9.1)\n• Performance: much better than 2PL (readers don't block writers)\n• Trade-off: higher abort rate bajo contention\n\n<pre><code>-- SSI in PostgreSQL:\nBEGIN ISOLATION LEVEL SERIALIZABLE;\n  SELECT COUNT(*) FROM doctors WHERE on_call = true AND shift_id = 123;\n  -- PostgreSQL tracks that this TX leer desde the doctors tabla\n  UPDATE doctors SET on_call = false WHERE id = 'alice' AND shift_id = 123;\nCOMMIT;\n-- If another TX modified filas that THIS TX leer → one gets aborted\n-- Application MUST reintentar: while (true) { try { doTransaction(); break; } catch (SerializationFailure) { reintentar; } }</code></pre>"
      },
      {
        "heading": "Guía práctica de decisión para entrevistas",
        "content": "<strong>Cuándo utilizarlo each aislamiento level:</strong>\n\n<strong>READ COMMITTED (PostgreSQL predeterminado):</strong>\n• Standard web CRUD applications\n• Most REST APIs\n• When leer-modify-escritura is done con atomic operations (UPDATE ... SET x = x + 1)\n• 90% of aplicación code lives here\n\n<strong>REPEATABLE READ / Snapshot Isolation:</strong>\n• Reporting consultas that necesitar a consistente point-in-time view\n• Backups (consistente instantánea)\n• Analytics consultas scanning grande datasets\n• Long-running lote jobs\n\n<strong>SERIALIZABLE:</strong>\n• Financial transacciones donde correctness > throughput\n• Booking systems (prevent double-booking)\n• Any escritura skew scenario the aplicación puede't handle\n• <strong>Always</strong> build reintentar logic cuando using SERIALIZABLE\n\n<strong>Application-level alternatives to SERIALIZABLE:</strong>\n<pre><code>-- 1. Explicit locking (FOR UPDATE)\nSELECT * FROM inventory WHERE product_id = 42 FOR UPDATE;\n-- Locks this fila, other TXs wait\n\n-- 2. Atomic operations (no leer-modify-escritura cycle)\nUPDATE inventory SET quantity = quantity - 1\nWHERE product_id = 42 AND quantity > 0;\n-- Single statement = atomic, no race condición\n\n-- 3. UNIQUE constraints (prevent duplicates)\nCREATE UNIQUE INDEX idx_booking_unique\n  ON bookings (room_id, time_slot);\n-- Database enforces uniqueness atomically</code></pre>\n\n<strong>REWE delivery sistema ejemplo:</strong>\n• Assigning driver to delivery: use SELECT FOR UPDATE on the driver fila\n• Confirming delivery: atomic UPDATE con WHERE status = 'IN_TRANSIT'\n• Generating daily reports: REPEATABLE READ to get consistente instantánea\n• Financial reconciliation: SERIALIZABLE (if possible) or explicit locking"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-isolation-mvcc"
  },
  {
    "id": "theory-dp-basics",
    "title": "Mapeo de entidades JPA — Del objeto a la tabla",
    "sections": [
      {
        "heading": "La incompatibilidad objeto-relacional",
        "content": "Relational databases store datos in tablas con foreign claves. Object-oriented code uses objeto graphs con references. The ORM bridges this gap:\n\n| Relational | OOP |\n|-----------|-----|\n| Table | Class |\n| Row | Instance |\n| Column | Field |\n| Foreign Key | Object reference |\n| JOIN | Navigation (getter) |\n\nJPA is the **specification** (interfaces, annotations). Hibernate is the **implementación** (real SQL generation, caching, dirty checking)."
      },
      {
        "heading": "Transiciones de estado de una entidad",
        "content": "Every JPA entidad exists in one of 4 estados:\n\n```\nNew (Transient)  →  persist()  →  Managed (Persistent)\n                                       ↓\n                                   detach() / close()\n                                       ↓\n                                   Detached\n                                       ↓\n                                   merge()  →  Managed\n\nManaged  →  remove()  →  Removed  →  flush()  →  DELETE in DB\n```\n\n**Managed** = entidad is tracked by the Persistence Context. Any field change is automáticamente detected (dirty checking) and will generate an UPDATE at flush time.\n\n**Detached** = entidad was gestionada but the EntityManager is closed. Changes are NOT tracked. Must call `merge()` to re-attach."
      },
      {
        "heading": "El contexto de persistencia (caché de primer nivel)",
        "content": "The EntityManager maintains a **Persistence Context** — an in-memoria map of all gestionada entidades.\n\n**Key behaviors:**\n1. **Garantía de identidad:** `em.buscar(Post.class, 1)` called twice devuelve the SAME Java objeto\n2. **Escritura diferida:** SQL is deferred until flush (transacción commit or explicit flush)\n3. **Detección de cambios:** Hibernate compares real estado vs. loaded estado at flush time\n4. **Repeatable leer:** Within the mismo PC, you siempre see the mismo datos (even if DB changed)\n\n```java\nPost post = em.buscar(Post.class, 1L); // SELECT → entidad cached in PC\npost.setTitle(\"New Title\");           // no SQL yet!\nem.flush();                           // NOW: UPDATE post SET title='New Title' WHERE id=1\n```\n\n**Warning:** The PC grows con cada gestionada entidad. For lote processing, flush+clear periodically to evitar OutOfMemoryError."
      },
      {
        "heading": "Estrategias de generación de identificadores",
        "content": "| Strategy | How | Batching? | Recommended? |\n|----------|-----|:---------:|:------------:|\n| `IDENTITY` | DB auto-increment | ❌ No | Avoid for alto-escritura |\n| `SEQUENCE` | DB sequence | ✅ Yes | ✅ Best choice |\n| `TABLE` | Separate tabla | ✅ Yes | ❌ Poor rendimiento |\n| `UUID` | Application-generated | ✅ Yes | Adecuado para distributed |\n\n**Why IDENTITY kills batching:** Hibernate debe execute INSERT inmediatamente to get the generated ID (necesario for PC identity map). This prevents grouping inserts en a lote.\n\n**Why SEQUENCE is mejor:** Hibernate puede pre-allocate IDs (allocationSize=50) in a single DB call, then assign them in-memoria sin extra roundtrips.\n\n```java\n@Id\n@GeneratedValue(estrategia = GenerationType.SEQUENCE, generator = \"post_seq\")\n@SequenceGenerator(name = \"post_seq\", sequenceName = \"post_seq\", allocationSize = 50)\nprivate Long id;\n```"
      },
      {
        "heading": "Fundamentos del mapeo",
        "content": "```java\n@Entity\n@Table(name = \"post\")  // explicit tabla name\npublic class Post {\n\n    @Id\n    @GeneratedValue(estrategia = GenerationType.SEQUENCE)\n    private Long id;\n\n    @Column(nullable = false, length = 255)\n    private String title;\n\n    @Version  // optimistic locking\n    private int version;\n\n    @Enumerated(EnumType.STRING)  // nunca ORDINAL in producción\n    private PostStatus status;\n\n    @Column(updatable = false)  // set once, nunca changed\n    private LocalDateTime createdOn;\n\n    @Lob  // grande text\n    private String content;\n}\n```\n\n**Rules:**\n- Always use `@Version` for optimistic locking in web apps\n- `EnumType.STRING` — nunca `ORDINAL` (breaks cuando enum order changes)\n- Prefer `LocalDateTime` sobre `Date` (immutable, no timezone confusion)\n- `FetchType.LAZY` is the predeterminado for collections — keep it that way"
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-jpa-fundamentals"
  },
  {
    "id": "theory-dp-relations",
    "title": "Relaciones y estrategias de carga — El campo minado del rendimiento",
    "sections": [
      {
        "heading": "Tipos de asociaciones",
        "content": "| Annotation | DB structure | Default Fetch |\n|-----------|-------------|:-------------:|\n| `@ManyToOne` | FK in child tabla | EAGER ⚠️ |\n| `@OneToMany` | FK in child tabla | LAZY ✅ |\n| `@OneToOne` | Shared PK or FK | EAGER ⚠️ |\n| `@ManyToMany` | Join tabla | LAZY ✅ |\n\n**Critical:** `@ManyToOne` defaults to EAGER. Always override:\n```java\n@ManyToOne(fetch = FetchType.LAZY)\n@JoinColumn(name = \"post_id\")\nprivate Post post;\n```\n\nFailing to set LAZY on `@ManyToOne` causes unexpected JOINs or secondary SELECTs on cada consulta that devuelve the child entidad."
      },
      {
        "heading": "El dilema de la carga",
        "content": "**Hard-wired carga (entidad annotations) is a trap.** Each use case needs diferente datos:\n\n- List view: Post title only (no comments necesario)\n- Detail view: Post + comments (JOIN FETCH)\n- Admin view: Post + comments + author profile\n\n**Rule:** Set everything to LAZY in the entidad, then elegir the carga estrategia por consulta:\n\n```java\n// ✅ Query-time carga — adapt to the use case\n// List view: no fetch\nSELECT p FROM Post p WHERE p.status = :status\n\n// Detail view: fetch comments\nSELECT p FROM Post p JOIN FETCH p.comments WHERE p.id = :id\n\n// Report: DTO projection (no entidad at all)\nSELECT nuevo PostSummaryDTO(p.id, p.title, SIZE(p.comments)) FROM Post p\n```"
      },
      {
        "heading": "Errores habituales de carga",
        "content": "**1. MultipleBagFetchException:**\nCannot JOIN FETCH two `List<>` collections simultáneamente (produces Cartesian product).\n```java\n// ❌ Throws MultipleBagFetchException\nSELECT p FROM Post p JOIN FETCH p.comments JOIN FETCH p.tags\n\n// ✅ Use Set<> en su lugar of List<>, or fetch in separate consultas\n```\n\n**2. Pagination + JOIN FETCH:**\n```java\n// ❌ Hibernate fetches ALL filas, then paginates in-memoria!\nQuery q = em.createQuery(\"SELECT p FROM Post p JOIN FETCH p.comments\");\nq.setFirstResult(0);\nq.setMaxResults(10);\n// WARNING: HHH000104: firstResult/maxResults con collection fetch\n```\nSolution: Paginate the parent consulta, then lote-fetch children con `@BatchSize(size=25)`.\n\n**3. N+1 in disguise:**\nAccessing a lazy field in a loop (e.g., in a JSF template, a Jackson serializer, or a `toString()` method) triggers N consultas silently."
      },
      {
        "heading": "@BatchSize — La solución sencilla para N+1",
        "content": "Instead of JOIN FETCH (which complicates pagination), use `@BatchSize` on the collection:\n\n```java\n@OneToMany(mappedBy = \"post\", fetch = FetchType.LAZY)\n@BatchSize(size = 25)  // load comments for 25 posts at once\nprivate List<Comment> comments;\n```\n\nWith `@BatchSize(25)`, accessing `post.getComments()` for the primero time loads comments for up to 25 already-loaded posts in a single `WHERE post_id IN (?, ?, ..., ?)` consulta.\n\n**Trade-off:** Not as optimal as JOIN FETCH for single-entidad fetches, but works cleanly con pagination and doesn't require consulta changes."
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-jpa-relations"
  },
  {
    "id": "theory-dp-queries",
    "title": "JPQL, Criteria y consultas nativas — Elegir la herramienta adecuada",
    "sections": [
      {
        "heading": "JPQL — SQL orientado a objetos",
        "content": "JPQL operates on **entidades and fields**, not tablas and columnas:\n\n```java\n// JPQL uses entidad names and field names\nSELECT p FROM Post p WHERE p.status = :status\n\n// vs SQL which uses tabla/columna names\nSELECT * FROM post WHERE status = ?\n```\n\n**Key features:**\n- Named parameters (`:status`) — prevents SQL injection, reusable\n- JOIN FETCH — solve N+1 at consulta time\n- Constructor expressions — `SELECT nuevo DTO(...)` for projections\n- Subqueries, aggregate functions, CASE expressions\n\n**Limitations:** No window functions, no CTEs, no base de datos-specific syntax."
      },
      {
        "heading": "Consultas nativas — Cuando JPQL no es suficiente",
        "content": "Use native SQL for:\n- Window functions (`ROW_NUMBER`, `DENSE_RANK`)\n- CTEs (Common Table Expressions)\n- Database-specific features (`LATERAL JOIN`, `DISTINCT ON`)\n- Complex reporting consultas\n- Performance-crítico consultas donde you necesitar full control\n\n```java\n@Query(valor = \"\"\"\n    SELECT id, title,\n           ROW_NUMBER() OVER (ORDER BY created_on DESC) as row_num\n    FROM post\n    WHERE status = :status\n    \"\"\", nativeQuery = true)\nList<Object[]> findRankedPosts(@Param(\"status\") String status);\n```\n\n**Warning:** Native consultas bypass the Persistence Context flush. Hibernate's `AUTO` flush mode does NOT flush antes native consultas (unlike JPQL). Call `em.flush()` manually if necesario."
      },
      {
        "heading": "Proyecciones DTO — La mejor opción para solo lectura",
        "content": "**Cuándo utilizarlo entidades:** You plan to modify the datos (UPDATE/DELETE).\n**Cuándo utilizarlo DTOs:** Solo lectura views (lists, reports, APIs).\n\n```java\n// ✅ Interface projection (Spring Data)\npublic interface PostSummary {\n    Long getId();\n    String getTitle();\n    int getCommentCount();\n}\n\n// ✅ Class projection (JPQL constructor)\n@Query(\"SELECT nuevo com.app.dto.PostSummaryDTO(p.id, p.title, SIZE(p.comments)) FROM Post p\")\nList<PostSummaryDTO> findSummaries();\n```\n\n**Performance advantage:** No dirty checking, no Persistence Context overhead, only selected columnas fetched desde DB. For a list of 1000 registros, this is significantly más rápido than carga 1000 full entidades."
      },
      {
        "heading": "Paginación — Offset frente a keyset",
        "content": "**Offset pagination** (standard JPA):\n```java\nquery.setFirstResult(offset);  // OFFSET\nquery.setMaxResults(limit);    // LIMIT\n```\nProblem: Database debe escaneo and discard `offset` filas. Page 1000 is much slower than página 1.\n\n**Keyset pagination** (no offset — use last seen clave):\n```sql\nSELECT * FROM post\nWHERE (created_on, id) < (:lastDate, :lastId)\nORDER BY created_on DESC, id DESC\nLIMIT 20\n```\n\n**Rule of thumb:**\n- < 10K total results → offset is fine\n- > 10K results → paginación por clave\n- Infinite scroll UI → siempre keyset\n\nJPA doesn't support keyset natively. Use native consulta or a library like jOOQ."
      },
      {
        "heading": "Criteria API — Construcción dinámica de consultas",
        "content": "Use Criteria API cuando the consulta structure depends on runtime condiciones (filters, search):\n\n```java\nCriteriaBuilder cb = em.getCriteriaBuilder();\nCriteriaQuery<Post> cq = cb.createQuery(Post.class);\nRoot<Post> raíz = cq.desde(Post.class);\n\nList<Predicate> predicates = nuevo ArrayList<>();\n\nif (status != null) {\n    predicates.add(cb.equal(raíz.get(\"status\"), status));\n}\nif (author != null) {\n    predicates.add(cb.equal(raíz.get(\"author\"), author));\n}\nif (fromDate != null) {\n    predicates.add(cb.greaterThanOrEqualTo(raíz.get(\"createdOn\"), fromDate));\n}\n\ncq.donde(predicates.toArray(nuevo Predicate[0]));\ncq.orderBy(cb.desc(raíz.get(\"createdOn\")));\n\nreturn em.createQuery(cq).setMaxResults(20).getResultList();\n```\n\n**Cuándo NO utilizarlo:** Static consultas known at compile time → JPQL is más readable."
      }
    ],
    "topic": "data-persistence",
    "subtopic": "dp-jpa-queries"
  },
  {
    "id": "theory-dp-hibernate-performance",
    "topic": "data-persistence",
    "subtopic": "dp-hibernate-performance",
    "title": "Rendimiento de Hibernate — N+1, procesamiento por lotes, caché y bloqueos",
    "sections": [
      {
        "heading": "El problema N+1",
        "content": "The most habitual JPA rendimiento issue. When carga a collection lazily, Hibernate executes 1 consulta for the parent + N consultas for each child.\n\n```java\n// ❌ N+1: 1 consulta for posts + N consultas for comments\nList<Post> posts = em.createQuery(\"SELECT p FROM Post p\", Post.class).getResultList();\nfor (Post p : posts) {\n    p.getComments().size(); // triggers lazy load → 1 SELECT por post\n}\n\n// ✅ JOIN FETCH: 1 single consulta\nList<Post> posts = em.createQuery(\n    \"SELECT p FROM Post p JOIN FETCH p.comments\", Post.class\n).getResultList();\n```\n\n**Detection:** Enable `hibernate.generate_statistics=true` and look for alto consulta counts relative to entidad counts."
      },
      {
        "heading": "Procesamiento por lotes JDBC",
        "content": "Hibernate puede group INSERT/UPDATE/DELETE statements en batches, reducing base de datos roundtrips.\n\n```properties\n# Enable batching (recommended: 10-30)\nhibernate.jdbc.batch_size=25\nhibernate.order_inserts=true\nhibernate.order_updates=true\n```\n\n**Key rules:**\n- `IDENTITY` generators **disable** insert batching (Hibernate needs the ID inmediatamente)\n- Use `SEQUENCE` generators con `allocationSize > 1` for lote-friendly inserts\n- Batch size 10-30 is the sweet spot — larger is not linearly better\n- Mixing entidad types in the mismo flush breaks batching unless `order_inserts=true`"
      },
      {
        "heading": "El contexto de persistencia como caché de escritura diferida",
        "content": "The EntityManager (1st-level caché) defers all SQL until flush time. This means:\n\n1. **Reduced bloqueo time** — bloqueos acquired only at flush, not at each `persist()`\n2. **Batch opportunity** — all statements available at once for batching\n3. **Detección de cambios** — only changed fields generate UPDATE\n\n**Pitfall:** The Persistence Context grows con cada gestionada entidad. In lote processing, flush + clear periodically:\n\n```java\nfor (int i = 0; i < 10_000; i++) {\n    em.persist(entidad);\n    if (i % 50 == 0) {\n        em.flush();\n        em.clear(); // detach all — free memoria\n    }\n}\n```"
      },
      {
        "heading": "Caché de segundo nivel",
        "content": "The 2nd-level caché (L2) is shared entre sessions/transacciones. Candidates: entidades leer frequently, written rara vez.\n\n| Strategy | Use case |\n|----------|----------|\n| `READ_ONLY` | Immutable reference datos (countries, enums) |\n| `READ_WRITE` | Read-heavy, occasional escrituras (products, categories) |\n| `NONSTRICT_READ_WRITE` | Tolerates brief staleness |\n\n```java\n@Entity\n@Cacheable\n@Cache(usage = CacheConcurrencyStrategy.READ_WRITE)\npublic class Category { ... }\n```\n\n**Never caché:** alto-escritura entidades (orders, events, logs), grande collections, or entidades accessed by varias apps writing directamente to the DB."
      },
      {
        "heading": "Bloqueo optimista frente a pesimista",
        "content": "**Bloqueo optimista** (preferred for web apps):\n```java\n@Version\nprivate int version; // auto-incremented on UPDATE\n// Throws OptimisticLockException if version changed entre leer and escritura\n```\n\n**Bloqueo pesimista** (for crítico sections):\n```java\nem.buscar(Account.class, id, LockModeType.PESSIMISTIC_WRITE);\n// SELECT ... FOR UPDATE — blocks other transacciones\n```\n\n**Rule of thumb:** Use optimistic by predeterminado. Switch to pessimistic only cuando conflicto probability is alto AND reintentar cost is unacceptable."
      },
      {
        "heading": "Proyecciones en lugar de entidades",
        "content": "For leer-only views (reports, lists, APIs), fetch only qué you necesitar:\n\n```java\n// ❌ Loads full entidad graph en Persistence Context\nList<Order> orders = em.createQuery(\"FROM Order\", Order.class).getResultList();\n\n// ✅ DTO projection — no gestionada entidades, no dirty checking overhead\nList<OrderSummaryDTO> summaries = em.createQuery(\n    \"SELECT nuevo OrderSummaryDTO(o.id, o.total, o.status) FROM Order o\",\n    OrderSummaryDTO.class\n).getResultList();\n```\n\n**Guideline:** Fetch entidades only cuando you plan to modify them. For everything else, use projections."
      }
    ]
  }
]